# Auto-generado por Knowledge Generator — NO incorporar directamente
# Revisar antes de agregar al proyecto de tests

from __future__ import annotations


# ---------------------------------------------------------------------------
# Tests para nuevas reglas semánticas
# ---------------------------------------------------------------------------


def test_suelo_preparacion_match():
    """Debería activarse para cuentas con: suelo, preparacion, camellones"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'suelo_preparacion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de suelo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'suelo_preparacion' debería activarse para 'Cuenta de suelo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de preparacion Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'suelo_preparacion' debería activarse para 'Cuenta de preparacion Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_suelo_preparacion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'suelo_preparacion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_administracion_gastos_match():
    """Debería activarse para cuentas con: administracion, gastos, generales"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'administracion_gastos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de administracion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'administracion_gastos' debería activarse para 'Cuenta de administracion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de gastos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'administracion_gastos' debería activarse para 'Cuenta de gastos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_administracion_gastos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'administracion_gastos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_administracion_sueldos_match():
    """Debería activarse para cuentas con: administracion, sueldos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'administracion_sueldos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de administracion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'administracion_sueldos' debería activarse para 'Cuenta de administracion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de sueldos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'administracion_sueldos' debería activarse para 'Cuenta de sueldos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_administracion_sueldos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'administracion_sueldos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_habilitacion_proyecto_match():
    """Debería activarse para cuentas con: habilitacion, proyecto, pre"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'habilitacion_proyecto'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de habilitacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'habilitacion_proyecto' debería activarse para 'Cuenta de habilitacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de proyecto Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'habilitacion_proyecto' debería activarse para 'Cuenta de proyecto Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_habilitacion_proyecto_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'habilitacion_proyecto'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_equipos_acum_match():
    """Debería activarse para cuentas con: equipos, acum, impulsiones"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'equipos_acum'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de equipos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'equipos_acum' debería activarse para 'Cuenta de equipos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de acum Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'equipos_acum' debería activarse para 'Cuenta de acum Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_equipos_acum_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'equipos_acum'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_diciembre_enero_match():
    """Debería activarse para cuentas con: diciembre, enero"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'diciembre_enero'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de diciembre Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'diciembre_enero' debería activarse para 'Cuenta de diciembre Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de enero Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'diciembre_enero' debería activarse para 'Cuenta de enero Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_diciembre_enero_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'diciembre_enero'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_corrientes_otros_match():
    """Debería activarse para cuentas con: corrientes, otros, activos, financieros"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'corrientes_otros'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de corrientes Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'corrientes_otros' debería activarse para 'Cuenta de corrientes Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de otros Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'corrientes_otros' debería activarse para 'Cuenta de otros Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_corrientes_otros_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'corrientes_otros'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_documentos_pagar_match():
    """Debería activarse para cuentas con: documentos, pagar, cobrar"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'documentos_pagar'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de documentos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'documentos_pagar' debería activarse para 'Cuenta de documentos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de pagar Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'documentos_pagar' debería activarse para 'Cuenta de pagar Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_documentos_pagar_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'documentos_pagar'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_materiales_oficina_match():
    """Debería activarse para cuentas con: materiales, oficina, aseo, gastos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'materiales_oficina'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de materiales Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'materiales_oficina' debería activarse para 'Cuenta de materiales Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de oficina Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'materiales_oficina' debería activarse para 'Cuenta de oficina Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_materiales_oficina_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'materiales_oficina'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_explotacion_margen_match():
    """Debería activarse para cuentas con: explotacion, margen, ingresos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'explotacion_margen'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de explotacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'explotacion_margen' debería activarse para 'Cuenta de explotacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de margen Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'explotacion_margen' debería activarse para 'Cuenta de margen Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_explotacion_margen_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'explotacion_margen'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_cuentas_comerciales_match():
    """Debería activarse para cuentas con: cuentas, comerciales, otras"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cuentas_comerciales'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de cuentas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'cuentas_comerciales' debería activarse para 'Cuenta de cuentas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de comerciales Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'cuentas_comerciales' debería activarse para 'Cuenta de comerciales Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_cuentas_comerciales_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cuentas_comerciales'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_ganancia_bruta_match():
    """Debería activarse para cuentas con: ganancia, bruta, antes"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ganancia_bruta'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de ganancia Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'ganancia_bruta' debería activarse para 'Cuenta de ganancia Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de bruta Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'ganancia_bruta' debería activarse para 'Cuenta de bruta Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_ganancia_bruta_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ganancia_bruta'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_servicio_correo_match():
    """Debería activarse para cuentas con: servicio, correo, seguridad, productos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'servicio_correo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de servicio Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'servicio_correo' debería activarse para 'Cuenta de servicio Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de correo Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'servicio_correo' debería activarse para 'Cuenta de correo Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_servicio_correo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'servicio_correo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_utilidad_perdida_match():
    """Debería activarse para cuentas con: utilidad, perdida, venta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'utilidad_perdida'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de utilidad Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'utilidad_perdida' debería activarse para 'Cuenta de utilidad Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de perdida Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'utilidad_perdida' debería activarse para 'Cuenta de perdida Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_utilidad_perdida_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'utilidad_perdida'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_cuentas_relacionadas_match():
    """Debería activarse para cuentas con: cuentas, relacionadas, entidades"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cuentas_relacionadas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de cuentas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'cuentas_relacionadas' debería activarse para 'Cuenta de cuentas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de relacionadas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'cuentas_relacionadas' debería activarse para 'Cuenta de relacionadas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_cuentas_relacionadas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cuentas_relacionadas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_otros_gastos_match():
    """Debería activarse para cuentas con: otros, gastos, personal, generales"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'otros_gastos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de otros Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'otros_gastos' debería activarse para 'Cuenta de otros Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de gastos Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'otros_gastos' debería activarse para 'Cuenta de gastos Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_otros_gastos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'otros_gastos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_remuneraciones_total_match():
    """Debería activarse para cuentas con: remuneraciones, total, anticipo, pasivo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'remuneraciones_total'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de remuneraciones Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'remuneraciones_total' debería activarse para 'Cuenta de remuneraciones Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de total Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'remuneraciones_total' debería activarse para 'Cuenta de total Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_remuneraciones_total_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'remuneraciones_total'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_efectivo_equivalentes_match():
    """Debería activarse para cuentas con: efectivo, equivalentes, capital"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'efectivo_equivalentes'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de efectivo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'efectivo_equivalentes' debería activarse para 'Cuenta de efectivo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de equivalentes Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'efectivo_equivalentes' debería activarse para 'Cuenta de equivalentes Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_efectivo_equivalentes_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'efectivo_equivalentes'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_nivel_anivel_match():
    """Debería activarse para cuentas con: nivel, anivel"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'nivel_anivel'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de nivel Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'nivel_anivel' debería activarse para 'Cuenta de nivel Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de anivel Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'nivel_anivel' debería activarse para 'Cuenta de anivel Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_nivel_anivel_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'nivel_anivel'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_indemnizacion_cta_match():
    """Debería activarse para cuentas con: indemnizacion, cta, pablo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'indemnizacion_cta'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de indemnizacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'indemnizacion_cta' debería activarse para 'Cuenta de indemnizacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cta Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'indemnizacion_cta' debería activarse para 'Cuenta de cta Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_indemnizacion_cta_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'indemnizacion_cta'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_moneda_pagina_match():
    """Debería activarse para cuentas con: moneda, pagina, peso, chileno"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'moneda_pagina'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de moneda Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'moneda_pagina' debería activarse para 'Cuenta de moneda Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de pagina Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'moneda_pagina' debería activarse para 'Cuenta de pagina Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_moneda_pagina_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'moneda_pagina'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_agua_administracion_match():
    """Debería activarse para cuentas con: agua, administracion, gastos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'agua_administracion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de agua Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'agua_administracion' debería activarse para 'Cuenta de agua Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de administracion Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'agua_administracion' debería activarse para 'Cuenta de administracion Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_agua_administracion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'agua_administracion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_pagar_cuentas_match():
    """Debería activarse para cuentas con: pagar, cuentas, empresa, caja"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pagar_cuentas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de pagar Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'pagar_cuentas' debería activarse para 'Cuenta de pagar Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cuentas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'pagar_cuentas' debería activarse para 'Cuenta de cuentas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_pagar_cuentas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pagar_cuentas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_capital_emitido_match():
    """Debería activarse para cuentas con: capital, emitido, jose"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'capital_emitido'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de capital Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'capital_emitido' debería activarse para 'Cuenta de capital Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de emitido Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'capital_emitido' debería activarse para 'Cuenta de emitido Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_capital_emitido_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'capital_emitido'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_pagina_asesorias_match():
    """Debería activarse para cuentas con: pagina, asesorias, inversiones"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pagina_asesorias'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de pagina Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'pagina_asesorias' debería activarse para 'Cuenta de pagina Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de asesorias Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'pagina_asesorias' debería activarse para 'Cuenta de asesorias Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_pagina_asesorias_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pagina_asesorias'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_ingresos_actividades_match():
    """Debería activarse para cuentas con: ingresos, actividades, ordinarias, buses"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ingresos_actividades'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de ingresos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'ingresos_actividades' debería activarse para 'Cuenta de ingresos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de actividades Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'ingresos_actividades' debería activarse para 'Cuenta de actividades Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_ingresos_actividades_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ingresos_actividades'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_diferidos_impuestos_match():
    """Debería activarse para cuentas con: diferidos, impuestos, diferido, impuesto"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'diferidos_impuestos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de diferidos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'diferidos_impuestos' debería activarse para 'Cuenta de diferidos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de impuestos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'diferidos_impuestos' debería activarse para 'Cuenta de impuestos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_diferidos_impuestos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'diferidos_impuestos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_total_activo_match():
    """Debería activarse para cuentas con: total, activo, fijo, patrimonio, neto"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'total_activo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de total Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'total_activo' debería activarse para 'Cuenta de total Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de activo Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'total_activo' debería activarse para 'Cuenta de activo Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_total_activo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'total_activo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_ppm_reajuste_match():
    """Debería activarse para cuentas con: ppm, reajuste, iva, ppmava"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ppm_reajuste'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de ppm Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'ppm_reajuste' debería activarse para 'Cuenta de ppm Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de reajuste Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'ppm_reajuste' debería activarse para 'Cuenta de reajuste Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_ppm_reajuste_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ppm_reajuste'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_computacion_equipos_match():
    """Debería activarse para cuentas con: computacion, equipos, colacion, total"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'computacion_equipos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de computacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'computacion_equipos' debería activarse para 'Cuenta de computacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de equipos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'computacion_equipos' debería activarse para 'Cuenta de equipos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_computacion_equipos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'computacion_equipos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_riego_equipo_match():
    """Debería activarse para cuentas con: riego, equipo, olivos, spa"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'riego_equipo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de riego Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'riego_equipo' debería activarse para 'Cuenta de riego Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de equipo Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'riego_equipo' debería activarse para 'Cuenta de equipo Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_riego_equipo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'riego_equipo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_comisiones_percibidas_match():
    """Debería activarse para cuentas con: comisiones, percibidas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'comisiones_percibidas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de comisiones Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'comisiones_percibidas' debería activarse para 'Cuenta de comisiones Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de percibidas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'comisiones_percibidas' debería activarse para 'Cuenta de percibidas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_comisiones_percibidas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'comisiones_percibidas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_folio_dias_match():
    """Debería activarse para cuentas con: folio, dias, 2014"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'folio_dias'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de folio Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'folio_dias' debería activarse para 'Cuenta de folio Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de dias Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'folio_dias' debería activarse para 'Cuenta de dias Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_folio_dias_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'folio_dias'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_rengo_338_match():
    """Debería activarse para cuentas con: rengo, 338, piso"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'rengo_338'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de rengo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'rengo_338' debería activarse para 'Cuenta de rengo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 338 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'rengo_338' debería activarse para 'Cuenta de 338 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_rengo_338_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'rengo_338'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_imprenta_gastos_match():
    """Debería activarse para cuentas con: imprenta, gastos, reproduccion"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'imprenta_gastos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de imprenta Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'imprenta_gastos' debería activarse para 'Cuenta de imprenta Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de gastos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'imprenta_gastos' debería activarse para 'Cuenta de gastos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_imprenta_gastos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'imprenta_gastos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_obligaciones_bancos_match():
    """Debería activarse para cuentas con: obligaciones, bancos, financieras"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'obligaciones_bancos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de obligaciones Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'obligaciones_bancos' debería activarse para 'Cuenta de obligaciones Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de bancos Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'obligaciones_bancos' debería activarse para 'Cuenta de bancos Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_obligaciones_bancos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'obligaciones_bancos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_tributario_republica_match():
    """Debería activarse para cuentas con: tributario, republica"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'tributario_republica'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de tributario Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'tributario_republica' debería activarse para 'Cuenta de tributario Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de republica Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'tributario_republica' debería activarse para 'Cuenta de republica Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_tributario_republica_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'tributario_republica'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_intereses_multas_match():
    """Debería activarse para cuentas con: intereses, multas, mas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'intereses_multas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de intereses Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'intereses_multas' debería activarse para 'Cuenta de intereses Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de multas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'intereses_multas' debería activarse para 'Cuenta de multas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_intereses_multas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'intereses_multas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_corte_soporte_match():
    """Debería activarse para cuentas con: corte, soporte, gastos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'corte_soporte'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de corte Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'corte_soporte' debería activarse para 'Cuenta de corte Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de soporte Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'corte_soporte' debería activarse para 'Cuenta de soporte Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_corte_soporte_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'corte_soporte'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_vehiculos_comercial_match():
    """Debería activarse para cuentas con: vehiculos, comercial, patente, gastos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'vehiculos_comercial'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de vehiculos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'vehiculos_comercial' debería activarse para 'Cuenta de vehiculos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de comercial Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'vehiculos_comercial' debería activarse para 'Cuenta de comercial Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_vehiculos_comercial_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'vehiculos_comercial'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_comunicaciones_bonificaciones_match():
    """Debería activarse para cuentas con: comunicaciones, bonificaciones, telefonos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'comunicaciones_bonificaciones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de comunicaciones Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'comunicaciones_bonificaciones' debería activarse para 'Cuenta de comunicaciones Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de bonificaciones Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'comunicaciones_bonificaciones' debería activarse para 'Cuenta de bonificaciones Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_comunicaciones_bonificaciones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'comunicaciones_bonificaciones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_credito_linea_match():
    """Debería activarse para cuentas con: credito, linea, cta, especial"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'credito_linea'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de credito Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'credito_linea' debería activarse para 'Cuenta de credito Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de linea Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'credito_linea' debería activarse para 'Cuenta de linea Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_credito_linea_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'credito_linea'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_octubre_consumo_match():
    """Debería activarse para cuentas con: octubre, consumo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'octubre_consumo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de octubre Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'octubre_consumo' debería activarse para 'Cuenta de octubre Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de consumo Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'octubre_consumo' debería activarse para 'Cuenta de consumo Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_octubre_consumo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'octubre_consumo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_plantacion_personal_match():
    """Debería activarse para cuentas con: plantacion, personal, manuel, torres, eirl"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_personal'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de plantacion Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'plantacion_personal' debería activarse para 'Cuenta de plantacion Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de personal Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'plantacion_personal' debería activarse para 'Cuenta de personal Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_plantacion_personal_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_personal'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_plantacion_contratista_match():
    """Debería activarse para cuentas con: plantacion, contratista, angelica, vasquez, cabezas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_contratista'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de plantacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'plantacion_contratista' debería activarse para 'Cuenta de plantacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de contratista Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'plantacion_contratista' debería activarse para 'Cuenta de contratista Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_plantacion_contratista_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_contratista'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_obras_ejecucion_match():
    """Debería activarse para cuentas con: obras, ejecucion, arica, antofagasta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'obras_ejecucion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de obras Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'obras_ejecucion' debería activarse para 'Cuenta de obras Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de ejecucion Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'obras_ejecucion' debería activarse para 'Cuenta de ejecucion Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_obras_ejecucion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'obras_ejecucion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_disponible_bancos_match():
    """Debería activarse para cuentas con: disponible, bancos, 463, instituciones"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'disponible_bancos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de disponible Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'disponible_bancos' debería activarse para 'Cuenta de disponible Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de bancos Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'disponible_bancos' debería activarse para 'Cuenta de bancos Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_disponible_bancos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'disponible_bancos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_amortizacion_depreciacion_match():
    """Debería activarse para cuentas con: amortizacion, depreciacion, costo, acumulada"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'amortizacion_depreciacion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de amortizacion Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'amortizacion_depreciacion' debería activarse para 'Cuenta de amortizacion Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de depreciacion Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'amortizacion_depreciacion' debería activarse para 'Cuenta de depreciacion Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_amortizacion_depreciacion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'amortizacion_depreciacion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_impuesto_saldo_match():
    """Debería activarse para cuentas con: impuesto, saldo, favor"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'impuesto_saldo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de impuesto Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'impuesto_saldo' debería activarse para 'Cuenta de impuesto Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de saldo Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'impuesto_saldo' debería activarse para 'Cuenta de saldo Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_impuesto_saldo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'impuesto_saldo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_perdidas_ganancias_match():
    """Debería activarse para cuentas con: perdidas, ganancias, acumuladas, ganancia"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'perdidas_ganancias'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de perdidas Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'perdidas_ganancias' debería activarse para 'Cuenta de perdidas Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de ganancias Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'perdidas_ganancias' debería activarse para 'Cuenta de ganancias Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_perdidas_ganancias_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'perdidas_ganancias'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_movilizacion_sueldos_match():
    """Debería activarse para cuentas con: movilizacion, sueldos, acentralizacion"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'movilizacion_sueldos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de movilizacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'movilizacion_sueldos' debería activarse para 'Cuenta de movilizacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de sueldos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'movilizacion_sueldos' debería activarse para 'Cuenta de sueldos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_movilizacion_sueldos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'movilizacion_sueldos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_inventarios_corrientes_match():
    """Debería activarse para cuentas con: inventarios, corrientes, inversiones, provisiones"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'inventarios_corrientes'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de inventarios Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'inventarios_corrientes' debería activarse para 'Cuenta de inventarios Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de corrientes Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'inventarios_corrientes' debería activarse para 'Cuenta de corrientes Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_inventarios_corrientes_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'inventarios_corrientes'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_pasivos_corrientes_match():
    """Debería activarse para cuentas con: pasivos, corrientes, impuestos, arrendamiento"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pasivos_corrientes'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de pasivos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'pasivos_corrientes' debería activarse para 'Cuenta de pasivos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de corrientes Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'pasivos_corrientes' debería activarse para 'Cuenta de corrientes Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_pasivos_corrientes_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pasivos_corrientes'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_muebles_utiles_match():
    """Debería activarse para cuentas con: muebles, utiles, 12351"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'muebles_utiles'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de muebles Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'muebles_utiles' debería activarse para 'Cuenta de muebles Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de utiles Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'muebles_utiles' debería activarse para 'Cuenta de utiles Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_muebles_utiles_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'muebles_utiles'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_plantacion_fertilizante_match():
    """Debería activarse para cuentas con: plantacion, fertilizante"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_fertilizante'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de plantacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'plantacion_fertilizante' debería activarse para 'Cuenta de plantacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de fertilizante Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'plantacion_fertilizante' debería activarse para 'Cuenta de fertilizante Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_plantacion_fertilizante_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_fertilizante'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_electricidad_gastos_match():
    """Debería activarse para cuentas con: electricidad, gastos, 1353750"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'electricidad_gastos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de electricidad Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'electricidad_gastos' debería activarse para 'Cuenta de electricidad Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de gastos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'electricidad_gastos' debería activarse para 'Cuenta de gastos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_electricidad_gastos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'electricidad_gastos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_fletes_fletespruta_match():
    """Debería activarse para cuentas con: fletes, fletespruta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'fletes_fletespruta'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de fletes Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'fletes_fletespruta' debería activarse para 'Cuenta de fletes Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de fletespruta Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'fletes_fletespruta' debería activarse para 'Cuenta de fletespruta Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_fletes_fletespruta_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'fletes_fletespruta'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_comuna_bulnes_match():
    """Debería activarse para cuentas con: comuna, bulnes, pagina"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'comuna_bulnes'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de comuna Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'comuna_bulnes' debería activarse para 'Cuenta de comuna Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de bulnes Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'comuna_bulnes' debería activarse para 'Cuenta de bulnes Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_comuna_bulnes_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'comuna_bulnes'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_pagar_total_match():
    """Debería activarse para cuentas con: pagar, total, lineas, disposicion"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pagar_total'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de pagar Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'pagar_total' debería activarse para 'Cuenta de pagar Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de total Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'pagar_total' debería activarse para 'Cuenta de total Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_pagar_total_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pagar_total'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_acumuladas_ganancias_match():
    """Debería activarse para cuentas con: acumuladas, ganancias, perdida, ganacias"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'acumuladas_ganancias'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de acumuladas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'acumuladas_ganancias' debería activarse para 'Cuenta de acumuladas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de ganancias Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'acumuladas_ganancias' debería activarse para 'Cuenta de ganancias Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_acumuladas_ganancias_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'acumuladas_ganancias'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_plantacion_fertilizante_match():
    """Debería activarse para cuentas con: plantacion, fertilizante, copeval"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_fertilizante'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de plantacion Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'plantacion_fertilizante' debería activarse para 'Cuenta de plantacion Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de fertilizante Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'plantacion_fertilizante' debería activarse para 'Cuenta de fertilizante Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_plantacion_fertilizante_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_fertilizante'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_instalaciones_acum_match():
    """Debería activarse para cuentas con: instalaciones, acum, dep, roble"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'instalaciones_acum'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de instalaciones Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'instalaciones_acum' debería activarse para 'Cuenta de instalaciones Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de acum Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'instalaciones_acum' debería activarse para 'Cuenta de acum Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_instalaciones_acum_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'instalaciones_acum'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_ingresos_otros_match():
    """Debería activarse para cuentas con: ingresos, otros, funcion, fletes"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ingresos_otros'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de ingresos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'ingresos_otros' debería activarse para 'Cuenta de ingresos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de otros Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'ingresos_otros' debería activarse para 'Cuenta de otros Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_ingresos_otros_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ingresos_otros'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_impulsiones_tranques_match():
    """Debería activarse para cuentas con: impulsiones, tranques, orellana, fuentes"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'impulsiones_tranques'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de impulsiones Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'impulsiones_tranques' debería activarse para 'Cuenta de impulsiones Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de tranques Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'impulsiones_tranques' debería activarse para 'Cuenta de tranques Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_impulsiones_tranques_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'impulsiones_tranques'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_agricola_lechera_match():
    """Debería activarse para cuentas con: agricola, lechera, stgo, plantacion, fertilizante"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'agricola_lechera'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de agricola Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'agricola_lechera' debería activarse para 'Cuenta de agricola Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de lechera Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'agricola_lechera' debería activarse para 'Cuenta de lechera Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_agricola_lechera_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'agricola_lechera'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_inmob_spa_match():
    """Debería activarse para cuentas con: inmob, spa, don, cta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'inmob_spa'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de inmob Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'inmob_spa' debería activarse para 'Cuenta de inmob Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de spa Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'inmob_spa' debería activarse para 'Cuenta de spa Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_inmob_spa_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'inmob_spa'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_varios_dificil_match():
    """Debería activarse para cuentas con: varios, dificil, clasificacion, clasificados"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'varios_dificil'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de varios Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'varios_dificil' debería activarse para 'Cuenta de varios Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de dificil Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'varios_dificil' debería activarse para 'Cuenta de dificil Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_varios_dificil_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'varios_dificil'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_capacitacion_gastos_match():
    """Debería activarse para cuentas con: capacitacion, gastos, credito"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'capacitacion_gastos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de capacitacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'capacitacion_gastos' debería activarse para 'Cuenta de capacitacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de gastos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'capacitacion_gastos' debería activarse para 'Cuenta de gastos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_capacitacion_gastos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'capacitacion_gastos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_radio_2011_match():
    """Debería activarse para cuentas con: radio, 2011, sony, auto"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'radio_2011'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de radio Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'radio_2011' debería activarse para 'Cuenta de radio Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 2011 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'radio_2011' debería activarse para 'Cuenta de 2011 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_radio_2011_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'radio_2011'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_deudores_venta_match():
    """Debería activarse para cuentas con: deudores, venta, neto, usd"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'deudores_venta'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de deudores Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'deudores_venta' debería activarse para 'Cuenta de deudores Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de venta Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'deudores_venta' debería activarse para 'Cuenta de venta Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_deudores_venta_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'deudores_venta'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_vacaciones_proporcionales_match():
    """Debería activarse para cuentas con: vacaciones, proporcionales, provision, provisione"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'vacaciones_proporcionales'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de vacaciones Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'vacaciones_proporcionales' debería activarse para 'Cuenta de vacaciones Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de proporcionales Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'vacaciones_proporcionales' debería activarse para 'Cuenta de proporcionales Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_vacaciones_proporcionales_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'vacaciones_proporcionales'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_hidrocivil_match():
    """Debería activarse para cuentas con: hidrocivil"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'hidrocivil'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de hidrocivil Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'hidrocivil' debería activarse para 'Cuenta de hidrocivil Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0


def test_hidrocivil_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'hidrocivil'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_proyecto_electrico_match():
    """Debería activarse para cuentas con: proyecto, electrico, electrica, nuevo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'proyecto_electrico'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de proyecto Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'proyecto_electrico' debería activarse para 'Cuenta de proyecto Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de electrico Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'proyecto_electrico' debería activarse para 'Cuenta de electrico Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_proyecto_electrico_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'proyecto_electrico'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_rio_claro_match():
    """Debería activarse para cuentas con: rio, claro, ltda, plantacion, tutores"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'rio_claro'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de rio Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'rio_claro' debería activarse para 'Cuenta de rio Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de claro Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'rio_claro' debería activarse para 'Cuenta de claro Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_rio_claro_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'rio_claro'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_montevideo_match():
    """Debería activarse para cuentas con: montevideo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'montevideo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de montevideo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'montevideo' debería activarse para 'Cuenta de montevideo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0


def test_montevideo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'montevideo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_banco_pagar_match():
    """Debería activarse para cuentas con: banco, pagar, corpbanca, cuentas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'banco_pagar'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de banco Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'banco_pagar' debería activarse para 'Cuenta de banco Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de pagar Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'banco_pagar' debería activarse para 'Cuenta de pagar Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_banco_pagar_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'banco_pagar'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_profesionales_retencion_match():
    """Debería activarse para cuentas con: profesionales, retencion, cat, tarjeta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'profesionales_retencion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de profesionales Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'profesionales_retencion' debería activarse para 'Cuenta de profesionales Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de retencion Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'profesionales_retencion' debería activarse para 'Cuenta de retencion Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_profesionales_retencion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'profesionales_retencion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_letreros_publicitarios_match():
    """Debería activarse para cuentas con: letreros, publicitarios, aporte"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'letreros_publicitarios'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de letreros Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'letreros_publicitarios' debería activarse para 'Cuenta de letreros Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de publicitarios Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'letreros_publicitarios' debería activarse para 'Cuenta de publicitarios Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_letreros_publicitarios_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'letreros_publicitarios'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_correspondencia_tramites_match():
    """Debería activarse para cuentas con: correspondencia, tramites, franqueo, encomienda"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'correspondencia_tramites'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de correspondencia Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'correspondencia_tramites' debería activarse para 'Cuenta de correspondencia Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de tramites Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'correspondencia_tramites' debería activarse para 'Cuenta de tramites Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_correspondencia_tramites_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'correspondencia_tramites'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_ebitda_ebida_match():
    """Debería activarse para cuentas con: ebitda, ebida"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ebitda_ebida'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de ebitda Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'ebitda_ebida' debería activarse para 'Cuenta de ebitda Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de ebida Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'ebitda_ebida' debería activarse para 'Cuenta de ebida Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_ebitda_ebida_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ebitda_ebida'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_provisiones_cobrar_match():
    """Debería activarse para cuentas con: provisiones, cobrar, fisco, documetos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'provisiones_cobrar'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de provisiones Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'provisiones_cobrar' debería activarse para 'Cuenta de provisiones Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cobrar Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'provisiones_cobrar' debería activarse para 'Cuenta de cobrar Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_provisiones_cobrar_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'provisiones_cobrar'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_prevision_instituciones_match():
    """Debería activarse para cuentas con: prevision, instituciones, clientes, cuentas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'prevision_instituciones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de prevision Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'prevision_instituciones' debería activarse para 'Cuenta de prevision Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de instituciones Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'prevision_instituciones' debería activarse para 'Cuenta de instituciones Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_prevision_instituciones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'prevision_instituciones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_mantencion_vehiculos_match():
    """Debería activarse para cuentas con: mantencion, vehiculos, edificio"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mantencion_vehiculos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de mantencion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'mantencion_vehiculos' debería activarse para 'Cuenta de mantencion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de vehiculos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'mantencion_vehiculos' debería activarse para 'Cuenta de vehiculos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_mantencion_vehiculos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mantencion_vehiculos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_plantacion_desinfeccion_match():
    """Debería activarse para cuentas con: plantacion, desinfeccion"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_desinfeccion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de plantacion Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'plantacion_desinfeccion' debería activarse para 'Cuenta de plantacion Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de desinfeccion Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'plantacion_desinfeccion' debería activarse para 'Cuenta de desinfeccion Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_plantacion_desinfeccion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_desinfeccion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_contribuciones_match():
    """Debería activarse para cuentas con: contribuciones"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'contribuciones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de contribuciones Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'contribuciones' debería activarse para 'Cuenta de contribuciones Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0


def test_contribuciones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'contribuciones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_gastos_oficina_match():
    """Debería activarse para cuentas con: gastos, oficina, publicidad, comision"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_oficina'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de gastos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'gastos_oficina' debería activarse para 'Cuenta de gastos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de oficina Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'gastos_oficina' debería activarse para 'Cuenta de oficina Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_gastos_oficina_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_oficina'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_papel_regalo_match():
    """Debería activarse para cuentas con: papel, regalo, bolsas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'papel_regalo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de papel Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'papel_regalo' debería activarse para 'Cuenta de papel Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de regalo Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'papel_regalo' debería activarse para 'Cuenta de regalo Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_papel_regalo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'papel_regalo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_materiales_electricos_match():
    """Debería activarse para cuentas con: materiales, electricos, ulectricos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'materiales_electricos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de materiales Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'materiales_electricos' debería activarse para 'Cuenta de materiales Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de electricos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'materiales_electricos' debería activarse para 'Cuenta de electricos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_materiales_electricos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'materiales_electricos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_sueldos_viaticos_match():
    """Debería activarse para cuentas con: sueldos, viaticos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'sueldos_viaticos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de sueldos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'sueldos_viaticos' debería activarse para 'Cuenta de sueldos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de viaticos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'sueldos_viaticos' debería activarse para 'Cuenta de viaticos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_sueldos_viaticos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'sueldos_viaticos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_ventas_otras_match():
    """Debería activarse para cuentas con: ventas, otras, extranjeras, mercado, fuera"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ventas_otras'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de ventas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'ventas_otras' debería activarse para 'Cuenta de ventas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de otras Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'ventas_otras' debería activarse para 'Cuenta de otras Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_ventas_otras_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ventas_otras'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_retenciones_deudores_match():
    """Debería activarse para cuentas con: retenciones, deudores, varios, neto"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'retenciones_deudores'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de retenciones Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'retenciones_deudores' debería activarse para 'Cuenta de retenciones Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de deudores Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'retenciones_deudores' debería activarse para 'Cuenta de deudores Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_retenciones_deudores_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'retenciones_deudores'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_diciembre_match():
    """Debería activarse para cuentas con: diciembre"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'diciembre'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de diciembre Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'diciembre' debería activarse para 'Cuenta de diciembre Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0


def test_diciembre_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'diciembre'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_seguridad_accesorios_match():
    """Debería activarse para cuentas con: seguridad, accesorios, elem, merc"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'seguridad_accesorios'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de seguridad Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'seguridad_accesorios' debería activarse para 'Cuenta de seguridad Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de accesorios Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'seguridad_accesorios' debería activarse para 'Cuenta de accesorios Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_seguridad_accesorios_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'seguridad_accesorios'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_reserva_rev_match():
    """Debería activarse para cuentas con: reserva, rev, capital, spa"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'reserva_rev'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de reserva Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'reserva_rev' debería activarse para 'Cuenta de reserva Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de rev Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'reserva_rev' debería activarse para 'Cuenta de rev Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_reserva_rev_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'reserva_rev'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_plantas_plantacion_match():
    """Debería activarse para cuentas con: plantas, plantacion, paz, correa, pizarro"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantas_plantacion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de plantas Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'plantas_plantacion' debería activarse para 'Cuenta de plantas Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de plantacion Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'plantas_plantacion' debería activarse para 'Cuenta de plantacion Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_plantas_plantacion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantas_plantacion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_traspaso_cta_match():
    """Debería activarse para cuentas con: traspaso, cta, cte, entre, bancos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'traspaso_cta'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de traspaso Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'traspaso_cta' debería activarse para 'Cuenta de traspaso Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cta Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'traspaso_cta' debería activarse para 'Cuenta de cta Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_traspaso_cta_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'traspaso_cta'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_inmob_spa_match():
    """Debería activarse para cuentas con: inmob, spa, tab, latorre"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'inmob_spa'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de inmob Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'inmob_spa' debería activarse para 'Cuenta de inmob Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de spa Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'inmob_spa' debería activarse para 'Cuenta de spa Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_inmob_spa_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'inmob_spa'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_existencias_provisiones_match():
    """Debería activarse para cuentas con: existencias, provisiones, materiales, insumos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'existencias_provisiones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de existencias Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'existencias_provisiones' debería activarse para 'Cuenta de existencias Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de provisiones Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'existencias_provisiones' debería activarse para 'Cuenta de provisiones Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_existencias_provisiones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'existencias_provisiones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_unico_impto_match():
    """Debería activarse para cuentas con: unico, impto, tarjeta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'unico_impto'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de unico Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'unico_impto' debería activarse para 'Cuenta de unico Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de impto Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'unico_impto' debería activarse para 'Cuenta de impto Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_unico_impto_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'unico_impto'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_leasing_obligaciones_match():
    """Debería activarse para cuentas con: leasing, obligaciones, largo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'leasing_obligaciones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de leasing Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'leasing_obligaciones' debería activarse para 'Cuenta de leasing Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de obligaciones Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'leasing_obligaciones' debería activarse para 'Cuenta de obligaciones Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_leasing_obligaciones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'leasing_obligaciones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_ejercicio_utilidad_match():
    """Debería activarse para cuentas con: ejercicio, utilidad"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ejercicio_utilidad'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de ejercicio Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'ejercicio_utilidad' debería activarse para 'Cuenta de ejercicio Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de utilidad Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'ejercicio_utilidad' debería activarse para 'Cuenta de utilidad Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_ejercicio_utilidad_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ejercicio_utilidad'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_seguros_edificios_match():
    """Debería activarse para cuentas con: seguros, edificios, otros"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'seguros_edificios'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de seguros Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'seguros_edificios' debería activarse para 'Cuenta de seguros Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de edificios Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'seguros_edificios' debería activarse para 'Cuenta de edificios Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_seguros_edificios_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'seguros_edificios'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_riego_maldonado_match():
    """Debería activarse para cuentas con: riego, maldonado"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'riego_maldonado'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de riego Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'riego_maldonado' debería activarse para 'Cuenta de riego Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de maldonado Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'riego_maldonado' debería activarse para 'Cuenta de maldonado Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_riego_maldonado_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'riego_maldonado'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_plantacion_tutores_match():
    """Debería activarse para cuentas con: plantacion, tutores"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_tutores'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de plantacion Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'plantacion_tutores' debería activarse para 'Cuenta de plantacion Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de tutores Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'plantacion_tutores' debería activarse para 'Cuenta de tutores Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_plantacion_tutores_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_tutores'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_pagar_honorarios_match():
    """Debería activarse para cuentas con: pagar, honorarios, tarjeta, din"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pagar_honorarios'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de pagar Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'pagar_honorarios' debería activarse para 'Cuenta de pagar Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de honorarios Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'pagar_honorarios' debería activarse para 'Cuenta de honorarios Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_pagar_honorarios_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pagar_honorarios'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_mat_prepar_match():
    """Debería activarse para cuentas con: mat, prepar, vitrinas, visual"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mat_prepar'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de mat Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'mat_prepar' debería activarse para 'Cuenta de mat Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de prepar Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'mat_prepar' debería activarse para 'Cuenta de prepar Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_mat_prepar_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mat_prepar'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_aporte_empresa_match():
    """Debería activarse para cuentas con: aporte, empresa, afc"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'aporte_empresa'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de aporte Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'aporte_empresa' debería activarse para 'Cuenta de aporte Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de empresa Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'aporte_empresa' debería activarse para 'Cuenta de empresa Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_aporte_empresa_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'aporte_empresa'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_inmob_coronel_match():
    """Debería activarse para cuentas con: inmob, coronel, alvarado, spa"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'inmob_coronel'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de inmob Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'inmob_coronel' debería activarse para 'Cuenta de inmob Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de coronel Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'inmob_coronel' debería activarse para 'Cuenta de coronel Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_inmob_coronel_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'inmob_coronel'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_monetaria_correc_match():
    """Debería activarse para cuentas con: monetaria, correc, orreccion, rechazados, art"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'monetaria_correc'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de monetaria Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'monetaria_correc' debería activarse para 'Cuenta de monetaria Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de correc Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'monetaria_correc' debería activarse para 'Cuenta de correc Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_monetaria_correc_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'monetaria_correc'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_computacionales_gastos_match():
    """Debería activarse para cuentas con: computacionales, gastos, insumos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'computacionales_gastos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de computacionales Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'computacionales_gastos' debería activarse para 'Cuenta de computacionales Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de gastos Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'computacionales_gastos' debería activarse para 'Cuenta de gastos Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_computacionales_gastos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'computacionales_gastos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_agricola_jardin_match():
    """Debería activarse para cuentas con: agricola, jardin"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'agricola_jardin'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de agricola Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'agricola_jardin' debería activarse para 'Cuenta de agricola Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de jardin Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'agricola_jardin' debería activarse para 'Cuenta de jardin Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_agricola_jardin_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'agricola_jardin'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_pagina_match():
    """Debería activarse para cuentas con: pagina"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pagina'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de pagina Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'pagina' debería activarse para 'Cuenta de pagina Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0


def test_pagina_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pagina'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_form_match():
    """Debería activarse para cuentas con: form"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'form'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de form Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'form' debería activarse para 'Cuenta de form Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0


def test_form_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'form'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_reajuste_art_match():
    """Debería activarse para cuentas con: reajuste, art, menos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'reajuste_art'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de reajuste Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'reajuste_art' debería activarse para 'Cuenta de reajuste Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de art Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'reajuste_art' debería activarse para 'Cuenta de art Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_reajuste_art_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'reajuste_art'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_provision_intereses_match():
    """Debería activarse para cuentas con: provision, intereses, wages, seguro"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'provision_intereses'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de provision Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'provision_intereses' debería activarse para 'Cuenta de provision Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de intereses Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'provision_intereses' debería activarse para 'Cuenta de intereses Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_provision_intereses_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'provision_intereses'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_inversiones_cuanta_match():
    """Debería activarse para cuentas con: inversiones, cuanta, pagar"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'inversiones_cuanta'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de inversiones Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'inversiones_cuanta' debería activarse para 'Cuenta de inversiones Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cuanta Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'inversiones_cuanta' debería activarse para 'Cuenta de cuanta Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_inversiones_cuanta_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'inversiones_cuanta'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_plantaciones_cia_match():
    """Debería activarse para cuentas con: plantaciones, cia, forestales, garcia"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantaciones_cia'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de plantaciones Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'plantaciones_cia' debería activarse para 'Cuenta de plantaciones Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cia Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'plantaciones_cia' debería activarse para 'Cuenta de cia Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_plantaciones_cia_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantaciones_cia'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_valor_fondos_match():
    """Debería activarse para cuentas con: valor, fondos, mutuos, mayor, financiero"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'valor_fondos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de valor Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'valor_fondos' debería activarse para 'Cuenta de valor Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de fondos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'valor_fondos' debería activarse para 'Cuenta de fondos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_valor_fondos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'valor_fondos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_periodo_renta_match():
    """Debería activarse para cuentas con: periodo, renta, liquida, compras"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'periodo_renta'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de periodo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'periodo_renta' debería activarse para 'Cuenta de periodo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de renta Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'periodo_renta' debería activarse para 'Cuenta de renta Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_periodo_renta_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'periodo_renta'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_saldo_segun_match():
    """Debería activarse para cuentas con: saldo, segun, libro"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'saldo_segun'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de saldo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'saldo_segun' debería activarse para 'Cuenta de saldo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de segun Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'saldo_segun' debería activarse para 'Cuenta de segun Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_saldo_segun_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'saldo_segun'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_bonificacion_ant_match():
    """Debería activarse para cuentas con: bonificacion, ant, marzo, sept"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'bonificacion_ant'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de bonificacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'bonificacion_ant' debería activarse para 'Cuenta de bonificacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de ant Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'bonificacion_ant' debería activarse para 'Cuenta de ant Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_bonificacion_ant_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'bonificacion_ant'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_caseta_obras_match():
    """Debería activarse para cuentas con: caseta, obras, civiles, orellana"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'caseta_obras'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de caseta Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'caseta_obras' debería activarse para 'Cuenta de caseta Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de obras Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'caseta_obras' debería activarse para 'Cuenta de obras Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_caseta_obras_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'caseta_obras'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_2019_match():
    """Debería activarse para cuentas con: 2019"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '2019'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 2019 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '2019' debería activarse para 'Cuenta de 2019 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0


def test_2019_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '2019'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_ambientacion_audio_match():
    """Debería activarse para cuentas con: ambientacion, audio"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ambientacion_audio'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de ambientacion Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'ambientacion_audio' debería activarse para 'Cuenta de ambientacion Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de audio Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'ambientacion_audio' debería activarse para 'Cuenta de audio Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_ambientacion_audio_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ambientacion_audio'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_leasing_diferido_match():
    """Debería activarse para cuentas con: leasing, diferido, interes, intereses"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'leasing_diferido'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de leasing Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'leasing_diferido' debería activarse para 'Cuenta de leasing Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de diferido Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'leasing_diferido' debería activarse para 'Cuenta de diferido Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_leasing_diferido_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'leasing_diferido'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_acreedores_scotiabank_match():
    """Debería activarse para cuentas con: acreedores, scotiabank, varios, 754"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'acreedores_scotiabank'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de acreedores Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'acreedores_scotiabank' debería activarse para 'Cuenta de acreedores Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de scotiabank Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'acreedores_scotiabank' debería activarse para 'Cuenta de scotiabank Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_acreedores_scotiabank_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'acreedores_scotiabank'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_activos_pasivos_match():
    """Debería activarse para cuentas con: activos, pasivos, intangibles, distintos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'activos_pasivos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de activos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'activos_pasivos' debería activarse para 'Cuenta de activos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de pasivos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'activos_pasivos' debería activarse para 'Cuenta de pasivos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_activos_pasivos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'activos_pasivos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_empleados_provisiones_match():
    """Debería activarse para cuentas con: empleados, provisiones, corrientes"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'empleados_provisiones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de empleados Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'empleados_provisiones' debería activarse para 'Cuenta de empleados Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de provisiones Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'empleados_provisiones' debería activarse para 'Cuenta de provisiones Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_empleados_provisiones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'empleados_provisiones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_intangibles_activos_match():
    """Debería activarse para cuentas con: intangibles, activos, distintos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'intangibles_activos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de intangibles Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'intangibles_activos' debería activarse para 'Cuenta de intangibles Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de activos Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'intangibles_activos' debería activarse para 'Cuenta de activos Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_intangibles_activos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'intangibles_activos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_colon_proyecto_match():
    """Debería activarse para cuentas con: colon, proyecto"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'colon_proyecto'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de colon Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'colon_proyecto' debería activarse para 'Cuenta de colon Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de proyecto Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'colon_proyecto' debería activarse para 'Cuenta de proyecto Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_colon_proyecto_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'colon_proyecto'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_cuentas_cobrar_match():
    """Debería activarse para cuentas con: cuentas, cobrar, pagar, relac"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cuentas_cobrar'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de cuentas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'cuentas_cobrar' debería activarse para 'Cuenta de cuentas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cobrar Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'cuentas_cobrar' debería activarse para 'Cuenta de cobrar Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_cuentas_cobrar_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cuentas_cobrar'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_arriendos_devengados_match():
    """Debería activarse para cuentas con: arriendos, devengados, anticipado, arriendo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'arriendos_devengados'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de arriendos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'arriendos_devengados' debería activarse para 'Cuenta de arriendos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de devengados Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'arriendos_devengados' debería activarse para 'Cuenta de devengados Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_arriendos_devengados_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'arriendos_devengados'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_gastos_vehiculo_match():
    """Debería activarse para cuentas con: gastos, vehiculo, rendiciones, telefonicos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_vehiculo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de gastos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'gastos_vehiculo' debería activarse para 'Cuenta de gastos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de vehiculo Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'gastos_vehiculo' debería activarse para 'Cuenta de vehiculo Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_gastos_vehiculo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_vehiculo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_capital_propio_match():
    """Debería activarse para cuentas con: capital, propio, revaloriz"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'capital_propio'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de capital Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'capital_propio' debería activarse para 'Cuenta de capital Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de propio Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'capital_propio' debería activarse para 'Cuenta de propio Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_capital_propio_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'capital_propio'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_perdida_ejercicio_match():
    """Debería activarse para cuentas con: perdida, ejercicio, 21050utilidad, 2005, ajuste"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'perdida_ejercicio'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de perdida Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'perdida_ejercicio' debería activarse para 'Cuenta de perdida Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de ejercicio Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'perdida_ejercicio' debería activarse para 'Cuenta de ejercicio Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_perdida_ejercicio_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'perdida_ejercicio'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_empresas_inversiones_match():
    """Debería activarse para cuentas con: empresas, inversiones, relacionadas, relacionada"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'empresas_inversiones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de empresas Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'empresas_inversiones' debería activarse para 'Cuenta de empresas Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de inversiones Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'empresas_inversiones' debería activarse para 'Cuenta de inversiones Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_empresas_inversiones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'empresas_inversiones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_doctos_pagar_match():
    """Debería activarse para cuentas con: doctos, pagar, ctas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'doctos_pagar'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de doctos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'doctos_pagar' debería activarse para 'Cuenta de doctos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de pagar Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'doctos_pagar' debería activarse para 'Cuenta de pagar Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_doctos_pagar_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'doctos_pagar'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_cta_inmob_match():
    """Debería activarse para cuentas con: cta, inmob, don, part"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cta_inmob'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de cta Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'cta_inmob' debería activarse para 'Cuenta de cta Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de inmob Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'cta_inmob' debería activarse para 'Cuenta de inmob Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_cta_inmob_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cta_inmob'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_otras_provisiones_match():
    """Debería activarse para cuentas con: otras, provisiones, corto, plazo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'otras_provisiones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de otras Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'otras_provisiones' debería activarse para 'Cuenta de otras Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de provisiones Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'otras_provisiones' debería activarse para 'Cuenta de provisiones Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_otras_provisiones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'otras_provisiones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_notas_adjuntas_match():
    """Debería activarse para cuentas con: notas, adjuntas, numeros"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'notas_adjuntas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de notas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'notas_adjuntas' debería activarse para 'Cuenta de notas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de adjuntas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'notas_adjuntas' debería activarse para 'Cuenta de adjuntas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_notas_adjuntas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'notas_adjuntas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_estados_financieros_match():
    """Debería activarse para cuentas con: estados, financieros, forman, parte"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'estados_financieros'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de estados Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'estados_financieros' debería activarse para 'Cuenta de estados Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de financieros Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'estados_financieros' debería activarse para 'Cuenta de financieros Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_estados_financieros_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'estados_financieros'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_materias_primas_match():
    """Debería activarse para cuentas con: materias, primas, existencias, retenciones, 30270venta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'materias_primas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de materias Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'materias_primas' debería activarse para 'Cuenta de materias Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de primas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'materias_primas' debería activarse para 'Cuenta de primas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_materias_primas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'materias_primas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_banco_santander_match():
    """Debería activarse para cuentas con: banco, santander, prestamo, credito, linea"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'banco_santander'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de banco Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'banco_santander' debería activarse para 'Cuenta de banco Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de santander Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'banco_santander' debería activarse para 'Cuenta de santander Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_banco_santander_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'banco_santander'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_direccion_avenida_match():
    """Debería activarse para cuentas con: direccion, avenida, torres"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'direccion_avenida'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de direccion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'direccion_avenida' debería activarse para 'Cuenta de direccion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de avenida Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'direccion_avenida' debería activarse para 'Cuenta de avenida Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_direccion_avenida_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'direccion_avenida'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_giro_comercial_match():
    """Debería activarse para cuentas con: giro, comercial, codigo, agricola"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'giro_comercial'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de giro Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'giro_comercial' debería activarse para 'Cuenta de giro Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de comercial Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'giro_comercial' debería activarse para 'Cuenta de comercial Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_giro_comercial_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'giro_comercial'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_publicidad_gastos_match():
    """Debería activarse para cuentas con: publicidad, gastos, 10125, 49225"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'publicidad_gastos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de publicidad Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'publicidad_gastos' debería activarse para 'Cuenta de publicidad Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de gastos Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'publicidad_gastos' debería activarse para 'Cuenta de gastos Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_publicidad_gastos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'publicidad_gastos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_credito_intereses_match():
    """Debería activarse para cuentas con: credito, intereses, 40250intereses, pag"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'credito_intereses'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de credito Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'credito_intereses' debería activarse para 'Cuenta de credito Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de intereses Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'credito_intereses' debería activarse para 'Cuenta de intereses Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_credito_intereses_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'credito_intereses'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_declaracion_mas_match():
    """Debería activarse para cuentas con: declaracion, mas, reajustes, fuera"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'declaracion_mas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de declaracion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'declaracion_mas' debería activarse para 'Cuenta de declaracion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de mas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'declaracion_mas' debería activarse para 'Cuenta de mas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_declaracion_mas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'declaracion_mas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_terceros_servicios_match():
    """Debería activarse para cuentas con: terceros, servicios, honorarios"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'terceros_servicios'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de terceros Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'terceros_servicios' debería activarse para 'Cuenta de terceros Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de servicios Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'terceros_servicios' debería activarse para 'Cuenta de servicios Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_terceros_servicios_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'terceros_servicios'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_seguros_porpagar_match():
    """Debería activarse para cuentas con: seguros, porpagar, had"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'seguros_porpagar'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de seguros Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'seguros_porpagar' debería activarse para 'Cuenta de seguros Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de porpagar Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'seguros_porpagar' debería activarse para 'Cuenta de porpagar Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_seguros_porpagar_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'seguros_porpagar'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_impuestos_gasto_match():
    """Debería activarse para cuentas con: impuestos, gasto"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'impuestos_gasto'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de impuestos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'impuestos_gasto' debería activarse para 'Cuenta de impuestos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de gasto Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'impuestos_gasto' debería activarse para 'Cuenta de gasto Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_impuestos_gasto_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'impuestos_gasto'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_asesorias_profesionales_match():
    """Debería activarse para cuentas con: asesorias, profesionales, honorarios, 52202"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'asesorias_profesionales'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de asesorias Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'asesorias_profesionales' debería activarse para 'Cuenta de asesorias Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de profesionales Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'asesorias_profesionales' debería activarse para 'Cuenta de profesionales Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_asesorias_profesionales_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'asesorias_profesionales'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_derechos_usufructo_match():
    """Debería activarse para cuentas con: derechos, usufructo, saldo, 13401"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'derechos_usufructo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de derechos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'derechos_usufructo' debería activarse para 'Cuenta de derechos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de usufructo Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'derechos_usufructo' debería activarse para 'Cuenta de usufructo Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_derechos_usufructo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'derechos_usufructo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_ajuste_remanente_match():
    """Debería activarse para cuentas con: ajuste, remanente, credito, fiscal"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ajuste_remanente'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de ajuste Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'ajuste_remanente' debería activarse para 'Cuenta de ajuste Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de remanente Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'ajuste_remanente' debería activarse para 'Cuenta de remanente Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_ajuste_remanente_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ajuste_remanente'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_mahuidanche_lote_match():
    """Debería activarse para cuentas con: mahuidanche, lote"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mahuidanche_lote'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de mahuidanche Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'mahuidanche_lote' debería activarse para 'Cuenta de mahuidanche Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de lote Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'mahuidanche_lote' debería activarse para 'Cuenta de lote Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_mahuidanche_lote_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mahuidanche_lote'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_plantas_plantacion_match():
    """Debería activarse para cuentas con: plantas, plantacion, natividad, ltda, nogal"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantas_plantacion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de plantas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'plantas_plantacion' debería activarse para 'Cuenta de plantas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de plantacion Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'plantas_plantacion' debería activarse para 'Cuenta de plantacion Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_plantas_plantacion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantas_plantacion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_2020_match():
    """Debería activarse para cuentas con: 2020"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '2020'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 2020 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '2020' debería activarse para 'Cuenta de 2020 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0


def test_2020_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '2020'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_bonificacion_conaf_match():
    """Debería activarse para cuentas con: bonificacion, conaf, constancia, puyaral"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'bonificacion_conaf'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de bonificacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'bonificacion_conaf' debería activarse para 'Cuenta de bonificacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de conaf Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'bonificacion_conaf' debería activarse para 'Cuenta de conaf Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_bonificacion_conaf_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'bonificacion_conaf'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_bonificacion_ramon_match():
    """Debería activarse para cuentas con: bonificacion, ramon, pedro, ipa"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'bonificacion_ramon'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de bonificacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'bonificacion_ramon' debería activarse para 'Cuenta de bonificacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de ramon Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'bonificacion_ramon' debería activarse para 'Cuenta de ramon Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_bonificacion_ramon_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'bonificacion_ramon'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_arauco_arsuco_match():
    """Debería activarse para cuentas con: arauco, arsuco, coger"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'arauco_arsuco'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de arauco Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'arauco_arsuco' debería activarse para 'Cuenta de arauco Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de arsuco Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'arauco_arsuco' debería activarse para 'Cuenta de arsuco Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_arauco_arsuco_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'arauco_arsuco'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_189_pag_match():
    """Debería activarse para cuentas con: 189, pag, 230"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '189_pag'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 189 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '189_pag' debería activarse para 'Cuenta de 189 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de pag Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '189_pag' debería activarse para 'Cuenta de pag Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_189_pag_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '189_pag'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_intereses_pagados_match():
    """Debería activarse para cuentas con: intereses, pagados, adeudados"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'intereses_pagados'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de intereses Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'intereses_pagados' debería activarse para 'Cuenta de intereses Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de pagados Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'intereses_pagados' debería activarse para 'Cuenta de pagados Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_intereses_pagados_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'intereses_pagados'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_asesorias_servicios_match():
    """Debería activarse para cuentas con: asesorias, servicios, negocios, cta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'asesorias_servicios'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de asesorias Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'asesorias_servicios' debería activarse para 'Cuenta de asesorias Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de servicios Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'asesorias_servicios' debería activarse para 'Cuenta de servicios Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_asesorias_servicios_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'asesorias_servicios'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_suscripciones_inscripciones_match():
    """Debería activarse para cuentas con: suscripciones, inscripciones, 49233"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'suscripciones_inscripciones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de suscripciones Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'suscripciones_inscripciones' debería activarse para 'Cuenta de suscripciones Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de inscripciones Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'suscripciones_inscripciones' debería activarse para 'Cuenta de inscripciones Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_suscripciones_inscripciones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'suscripciones_inscripciones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_gastos_legales_match():
    """Debería activarse para cuentas con: gastos, legales, leasing, 40228"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_legales'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de gastos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'gastos_legales' debería activarse para 'Cuenta de gastos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de legales Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'gastos_legales' debería activarse para 'Cuenta de legales Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_gastos_legales_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_legales'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_asesoria_legal_match():
    """Debería activarse para cuentas con: asesoria, legal, agricola, contable"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'asesoria_legal'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de asesoria Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'asesoria_legal' debería activarse para 'Cuenta de asesoria Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de legal Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'asesoria_legal' debería activarse para 'Cuenta de legal Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_asesoria_legal_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'asesoria_legal'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_nacionales_capital_match():
    """Debería activarse para cuentas con: nacionales, capital, ventas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'nacionales_capital'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de nacionales Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'nacionales_capital' debería activarse para 'Cuenta de nacionales Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de capital Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'nacionales_capital' debería activarse para 'Cuenta de capital Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_nacionales_capital_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'nacionales_capital'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_capital_pagado_match():
    """Debería activarse para cuentas con: capital, pagado, 23101, deudores"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'capital_pagado'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de capital Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'capital_pagado' debería activarse para 'Cuenta de capital Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de pagado Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'capital_pagado' debería activarse para 'Cuenta de pagado Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_capital_pagado_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'capital_pagado'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_dec_2017_match():
    """Debería activarse para cuentas con: dec, 2017, assets, 2015"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'dec_2017'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de dec Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'dec_2017' debería activarse para 'Cuenta de dec Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 2017 Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'dec_2017' debería activarse para 'Cuenta de 2017 Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_dec_2017_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'dec_2017'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_impuesto_timbres_match():
    """Debería activarse para cuentas con: impuesto, timbres, primera, categoria"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'impuesto_timbres'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de impuesto Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'impuesto_timbres' debería activarse para 'Cuenta de impuesto Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de timbres Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'impuesto_timbres' debería activarse para 'Cuenta de timbres Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_impuesto_timbres_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'impuesto_timbres'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_mercaderias_impto_match():
    """Debería activarse para cuentas con: mercaderias, impto, unico, categ"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mercaderias_impto'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de mercaderias Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'mercaderias_impto' debería activarse para 'Cuenta de mercaderias Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de impto Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'mercaderias_impto' debería activarse para 'Cuenta de impto Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_mercaderias_impto_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mercaderias_impto'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_cta_part_match():
    """Debería activarse para cuentas con: cta, part, rafael, abad, garcia"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cta_part'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de cta Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'cta_part' debería activarse para 'Cuenta de cta Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de part Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'cta_part' debería activarse para 'Cuenta de part Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_cta_part_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cta_part'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_plazo_deposito_match():
    """Debería activarse para cuentas con: plazo, deposito"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plazo_deposito'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de plazo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'plazo_deposito' debería activarse para 'Cuenta de plazo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de deposito Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'plazo_deposito' debería activarse para 'Cuenta de deposito Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_plazo_deposito_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plazo_deposito'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_sondajes_match():
    """Debería activarse para cuentas con: sondajes"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'sondajes'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de sondajes Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'sondajes' debería activarse para 'Cuenta de sondajes Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0


def test_sondajes_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'sondajes'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_plantacion_desinfeccion_match():
    """Debería activarse para cuentas con: plantacion, desinfeccion, raices, futuro"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_desinfeccion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de plantacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'plantacion_desinfeccion' debería activarse para 'Cuenta de plantacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de desinfeccion Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'plantacion_desinfeccion' debería activarse para 'Cuenta de desinfeccion Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_plantacion_desinfeccion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_desinfeccion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_maderera_alto_match():
    """Debería activarse para cuentas con: maderera, alto, lonquen, ltda, plantacion"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'maderera_alto'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de maderera Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'maderera_alto' debería activarse para 'Cuenta de maderera Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de alto Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'maderera_alto' debería activarse para 'Cuenta de alto Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_maderera_alto_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'maderera_alto'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_plantacion_eliecer_match():
    """Debería activarse para cuentas con: plantacion, eliecer, carmen, perez"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_eliecer'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de plantacion Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'plantacion_eliecer' debería activarse para 'Cuenta de plantacion Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de eliecer Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'plantacion_eliecer' debería activarse para 'Cuenta de eliecer Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_plantacion_eliecer_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_eliecer'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_capital_social_match():
    """Debería activarse para cuentas con: capital, social, terrenos, 359, 764"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'capital_social'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de capital Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'capital_social' debería activarse para 'Cuenta de capital Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de social Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'capital_social' debería activarse para 'Cuenta de social Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_capital_social_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'capital_social'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_agregados_arados_match():
    """Debería activarse para cuentas con: agregados, arados, into, oonononannoroso, 65242484"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'agregados_arados'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de agregados Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'agregados_arados' debería activarse para 'Cuenta de agregados Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de arados Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'agregados_arados' debería activarse para 'Cuenta de arados Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_agregados_arados_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'agregados_arados'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_jahuel_aguas_match():
    """Debería activarse para cuentas con: jahuel, aguas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'jahuel_aguas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de jahuel Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'jahuel_aguas' debería activarse para 'Cuenta de jahuel Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de aguas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'jahuel_aguas' debería activarse para 'Cuenta de aguas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_jahuel_aguas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'jahuel_aguas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_diciembre_2018_match():
    """Debería activarse para cuentas con: diciembre, 2018, a131"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'diciembre_2018'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de diciembre Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'diciembre_2018' debería activarse para 'Cuenta de diciembre Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 2018 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'diciembre_2018' debería activarse para 'Cuenta de 2018 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_diciembre_2018_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'diciembre_2018'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_prestamos_terceros_match():
    """Debería activarse para cuentas con: prestamos, terceros, caja, compensacion"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'prestamos_terceros'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de prestamos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'prestamos_terceros' debería activarse para 'Cuenta de prestamos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de terceros Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'prestamos_terceros' debería activarse para 'Cuenta de terceros Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_prestamos_terceros_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'prestamos_terceros'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_dep_acum_match():
    """Debería activarse para cuentas con: dep, acum, thompson, crimson, thompsom"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'dep_acum'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de dep Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'dep_acum' debería activarse para 'Cuenta de dep Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de acum Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'dep_acum' debería activarse para 'Cuenta de acum Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_dep_acum_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'dep_acum'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_dep_acum_match():
    """Debería activarse para cuentas con: dep, acum, red, globe"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'dep_acum'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de dep Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'dep_acum' debería activarse para 'Cuenta de dep Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de acum Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'dep_acum' debería activarse para 'Cuenta de acum Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_dep_acum_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'dep_acum'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_resultados_ejercicios_match():
    """Debería activarse para cuentas con: resultados, ejercicios, resultado"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'resultados_ejercicios'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de resultados Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'resultados_ejercicios' debería activarse para 'Cuenta de resultados Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de ejercicios Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'resultados_ejercicios' debería activarse para 'Cuenta de ejercicios Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_resultados_ejercicios_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'resultados_ejercicios'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_ejercicio_anterior_match():
    """Debería activarse para cuentas con: ejercicio, anterior, negativo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ejercicio_anterior'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de ejercicio Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'ejercicio_anterior' debería activarse para 'Cuenta de ejercicio Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de anterior Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'ejercicio_anterior' debería activarse para 'Cuenta de anterior Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_ejercicio_anterior_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ejercicio_anterior'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_anticipo_sueldos_match():
    """Debería activarse para cuentas con: anticipo, sueldos, exceso, sulldos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'anticipo_sueldos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de anticipo Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'anticipo_sueldos' debería activarse para 'Cuenta de anticipo Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de sueldos Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'anticipo_sueldos' debería activarse para 'Cuenta de sueldos Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_anticipo_sueldos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'anticipo_sueldos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_actualizacion_unidad_match():
    """Debería activarse para cuentas con: actualizacion, unidad"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'actualizacion_unidad'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de actualizacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'actualizacion_unidad' debería activarse para 'Cuenta de actualizacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de unidad Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'actualizacion_unidad' debería activarse para 'Cuenta de unidad Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_actualizacion_unidad_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'actualizacion_unidad'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_oscar_prohens_match():
    """Debería activarse para cuentas con: oscar, prohens, espinosa, pagina"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'oscar_prohens'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de oscar Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'oscar_prohens' debería activarse para 'Cuenta de oscar Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de prohens Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'oscar_prohens' debería activarse para 'Cuenta de prohens Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_oscar_prohens_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'oscar_prohens'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_suministros_colacion_match():
    """Debería activarse para cuentas con: suministros, colacion, otros, ali"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'suministros_colacion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de suministros Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'suministros_colacion' debería activarse para 'Cuenta de suministros Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de colacion Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'suministros_colacion' debería activarse para 'Cuenta de colacion Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_suministros_colacion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'suministros_colacion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_herbicidas_fungicidas_match():
    """Debería activarse para cuentas con: herbicidas, fungicidas, pesticida"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'herbicidas_fungicidas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de herbicidas Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'herbicidas_fungicidas' debería activarse para 'Cuenta de herbicidas Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de fungicidas Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'herbicidas_fungicidas' debería activarse para 'Cuenta de fungicidas Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_herbicidas_fungicidas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'herbicidas_fungicidas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_electrica_energia_match():
    """Debería activarse para cuentas con: electrica, energia, unurgia"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'electrica_energia'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de electrica Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'electrica_energia' debería activarse para 'Cuenta de electrica Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de energia Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'electrica_energia' debería activarse para 'Cuenta de energia Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_electrica_energia_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'electrica_energia'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_escritorio_utiles_match():
    """Debería activarse para cuentas con: escritorio, utiles, gtos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'escritorio_utiles'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de escritorio Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'escritorio_utiles' debería activarse para 'Cuenta de escritorio Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de utiles Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'escritorio_utiles' debería activarse para 'Cuenta de utiles Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_escritorio_utiles_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'escritorio_utiles'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_horas_extras_match():
    """Debería activarse para cuentas con: horas, extras"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'horas_extras'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de horas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'horas_extras' debería activarse para 'Cuenta de horas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de extras Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'horas_extras' debería activarse para 'Cuenta de extras Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_horas_extras_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'horas_extras'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_pagare_ltda_match():
    """Debería activarse para cuentas con: pagare, ltda, maravilla, inv, inversiones"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pagare_ltda'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de pagare Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'pagare_ltda' debería activarse para 'Cuenta de pagare Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de ltda Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'pagare_ltda' debería activarse para 'Cuenta de ltda Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_pagare_ltda_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pagare_ltda'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_gastos_cosecha_match():
    """Debería activarse para cuentas con: gastos, cosecha, 50830reasignacion, vendimia"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_cosecha'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de gastos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'gastos_cosecha' debería activarse para 'Cuenta de gastos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cosecha Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'gastos_cosecha' debería activarse para 'Cuenta de cosecha Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_gastos_cosecha_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_cosecha'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_deudas_recuperadas_match():
    """Debería activarse para cuentas con: deudas, recuperadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'deudas_recuperadas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de deudas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'deudas_recuperadas' debería activarse para 'Cuenta de deudas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de recuperadas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'deudas_recuperadas' debería activarse para 'Cuenta de recuperadas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_deudas_recuperadas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'deudas_recuperadas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_legales_gastos_match():
    """Debería activarse para cuentas con: legales, gastos, resery, reservas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'legales_gastos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de legales Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'legales_gastos' debería activarse para 'Cuenta de legales Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de gastos Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'legales_gastos' debería activarse para 'Cuenta de gastos Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_legales_gastos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'legales_gastos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_publicidad_difusion_match():
    """Debería activarse para cuentas con: publicidad, difusion, 279, 997"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'publicidad_difusion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de publicidad Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'publicidad_difusion' debería activarse para 'Cuenta de publicidad Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de difusion Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'publicidad_difusion' debería activarse para 'Cuenta de difusion Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_publicidad_difusion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'publicidad_difusion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_ingresos_ordinarios_match():
    """Debería activarse para cuentas con: ingresos, ordinarios, intereses, moratorios"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ingresos_ordinarios'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de ingresos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'ingresos_ordinarios' debería activarse para 'Cuenta de ingresos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de ordinarios Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'ingresos_ordinarios' debería activarse para 'Cuenta de ordinarios Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_ingresos_ordinarios_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ingresos_ordinarios'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_provision_impto_match():
    """Debería activarse para cuentas con: provision, impto, gasto, importacion"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'provision_impto'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de provision Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'provision_impto' debería activarse para 'Cuenta de provision Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de impto Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'provision_impto' debería activarse para 'Cuenta de impto Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_provision_impto_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'provision_impto'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_activo_inmovilizado_match():
    """Debería activarse para cuentas con: activo, inmovilizado, depreciacion, 50901, 2000"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'activo_inmovilizado'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de activo Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'activo_inmovilizado' debería activarse para 'Cuenta de activo Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de inmovilizado Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'activo_inmovilizado' debería activarse para 'Cuenta de inmovilizado Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_activo_inmovilizado_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'activo_inmovilizado'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_252_700_match():
    """Debería activarse para cuentas con: 252, 700, pag"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '252_700'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 252 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '252_700' debería activarse para 'Cuenta de 252 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 700 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '252_700' debería activarse para 'Cuenta de 700 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_252_700_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '252_700'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_resultados_acumulados_match():
    """Debería activarse para cuentas con: resultados, acumulados, 218, 029"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'resultados_acumulados'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de resultados Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'resultados_acumulados' debería activarse para 'Cuenta de resultados Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de acumulados Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'resultados_acumulados' debería activarse para 'Cuenta de acumulados Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_resultados_acumulados_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'resultados_acumulados'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_spa_plantacion_match():
    """Debería activarse para cuentas con: spa, plantacion, fertilizante, futuro, agro"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'spa_plantacion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de spa Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'spa_plantacion' debería activarse para 'Cuenta de spa Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de plantacion Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'spa_plantacion' debería activarse para 'Cuenta de plantacion Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_spa_plantacion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'spa_plantacion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_tractores_maldonado_match():
    """Debería activarse para cuentas con: tractores, maldonado, plantacion"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'tractores_maldonado'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de tractores Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'tractores_maldonado' debería activarse para 'Cuenta de tractores Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de maldonado Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'tractores_maldonado' debería activarse para 'Cuenta de maldonado Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_tractores_maldonado_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'tractores_maldonado'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_cta_cte_match():
    """Debería activarse para cuentas con: cta, cte, garcia"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cta_cte'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de cta Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'cta_cte' debería activarse para 'Cuenta de cta Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cte Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'cta_cte' debería activarse para 'Cuenta de cte Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_cta_cte_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cta_cte'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_arriendos_pagados_match():
    """Debería activarse para cuentas con: arriendos, pagados, leasing, anticipa"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'arriendos_pagados'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de arriendos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'arriendos_pagados' debería activarse para 'Cuenta de arriendos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de pagados Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'arriendos_pagados' debería activarse para 'Cuenta de pagados Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_arriendos_pagados_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'arriendos_pagados'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_desahucio_50090indemnizacion_match():
    """Debería activarse para cuentas con: desahucio, 50090indemnizacion, otros, honorarios, insumos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'desahucio_50090indemnizacion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de desahucio Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'desahucio_50090indemnizacion' debería activarse para 'Cuenta de desahucio Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 50090indemnizacion Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'desahucio_50090indemnizacion' debería activarse para 'Cuenta de 50090indemnizacion Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_desahucio_50090indemnizacion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'desahucio_50090indemnizacion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_oficinas_subcontratacion_match():
    """Debería activarse para cuentas con: oficinas, subcontratacion, externas, 10112"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'oficinas_subcontratacion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de oficinas Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'oficinas_subcontratacion' debería activarse para 'Cuenta de oficinas Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de subcontratacion Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'oficinas_subcontratacion' debería activarse para 'Cuenta de subcontratacion Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_oficinas_subcontratacion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'oficinas_subcontratacion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_categoria_imuesto_match():
    """Debería activarse para cuentas con: categoria, imuesto"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'categoria_imuesto'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de categoria Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'categoria_imuesto' debería activarse para 'Cuenta de categoria Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de imuesto Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'categoria_imuesto' debería activarse para 'Cuenta de imuesto Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_categoria_imuesto_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'categoria_imuesto'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_siembras_siembra_match():
    """Debería activarse para cuentas con: siembras, siembra, maiz, semillas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'siembras_siembra'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de siembras Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'siembras_siembra' debería activarse para 'Cuenta de siembras Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de siembra Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'siembras_siembra' debería activarse para 'Cuenta de siembra Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_siembras_siembra_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'siembras_siembra'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_pagina_altosa_match():
    """Debería activarse para cuentas con: pagina, altosa, lotal"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pagina_altosa'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de pagina Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'pagina_altosa' debería activarse para 'Cuenta de pagina Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de altosa Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'pagina_altosa' debería activarse para 'Cuenta de altosa Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_pagina_altosa_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pagina_altosa'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_114_959_match():
    """Debería activarse para cuentas con: 114, 959, pagina"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '114_959'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 114 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '114_959' debería activarse para 'Cuenta de 114 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 959 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '114_959' debería activarse para 'Cuenta de 959 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_114_959_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '114_959'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_gas_licuado_match():
    """Debería activarse para cuentas con: gas, licuado"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gas_licuado'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de gas Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'gas_licuado' debería activarse para 'Cuenta de gas Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de licuado Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'gas_licuado' debería activarse para 'Cuenta de licuado Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_gas_licuado_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gas_licuado'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_peajes_127_match():
    """Debería activarse para cuentas con: peajes, 127, 329"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'peajes_127'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de peajes Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'peajes_127' debería activarse para 'Cuenta de peajes Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 127 Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'peajes_127' debería activarse para 'Cuenta de 127 Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_peajes_127_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'peajes_127'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_tipo_cambio_match():
    """Debería activarse para cuentas con: tipo, cambio, 30490difererencia"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'tipo_cambio'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de tipo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'tipo_cambio' debería activarse para 'Cuenta de tipo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cambio Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'tipo_cambio' debería activarse para 'Cuenta de cambio Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_tipo_cambio_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'tipo_cambio'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_deferred_taxes_match():
    """Debería activarse para cuentas con: deferred, taxes, reserves, 461"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'deferred_taxes'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de deferred Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'deferred_taxes' debería activarse para 'Cuenta de deferred Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de taxes Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'deferred_taxes' debería activarse para 'Cuenta de taxes Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_deferred_taxes_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'deferred_taxes'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_cash_term_match():
    """Debería activarse para cuentas con: cash, term, debt, and"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cash_term'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de cash Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'cash_term' debería activarse para 'Cuenta de cash Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de term Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'cash_term' debería activarse para 'Cuenta de term Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_cash_term_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cash_term'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_comisiones_pagar_match():
    """Debería activarse para cuentas con: comisiones, pagar, cuentas, cobrar"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'comisiones_pagar'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de comisiones Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'comisiones_pagar' debería activarse para 'Cuenta de comisiones Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de pagar Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'comisiones_pagar' debería activarse para 'Cuenta de pagar Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_comisiones_pagar_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'comisiones_pagar'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_otras_retenciones_match():
    """Debería activarse para cuentas con: otras, retenciones, personal, acciones"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'otras_retenciones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de otras Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'otras_retenciones' debería activarse para 'Cuenta de otras Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de retenciones Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'otras_retenciones' debería activarse para 'Cuenta de retenciones Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_otras_retenciones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'otras_retenciones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_gastos_informatica_match():
    """Debería activarse para cuentas con: gastos, informatica, fotocopia"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_informatica'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de gastos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'gastos_informatica' debería activarse para 'Cuenta de gastos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de informatica Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'gastos_informatica' debería activarse para 'Cuenta de informatica Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_gastos_informatica_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_informatica'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_corp_banca_match():
    """Debería activarse para cuentas con: corp, banca, angeles, cuentas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'corp_banca'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de corp Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'corp_banca' debería activarse para 'Cuenta de corp Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de banca Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'corp_banca' debería activarse para 'Cuenta de banca Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_corp_banca_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'corp_banca'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_plantaciones_agricolas_match():
    """Debería activarse para cuentas con: plantaciones, agricolas, com"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantaciones_agricolas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de plantaciones Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'plantaciones_agricolas' debería activarse para 'Cuenta de plantaciones Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de agricolas Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'plantaciones_agricolas' debería activarse para 'Cuenta de agricolas Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_plantaciones_agricolas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantaciones_agricolas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_vehiculos_motorizados_match():
    """Debería activarse para cuentas con: vehiculos, motorizados, 12301, 12503"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'vehiculos_motorizados'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de vehiculos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'vehiculos_motorizados' debería activarse para 'Cuenta de vehiculos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de motorizados Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'vehiculos_motorizados' debería activarse para 'Cuenta de motorizados Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_vehiculos_motorizados_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'vehiculos_motorizados'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_tributaria_inversiones_match():
    """Debería activarse para cuentas con: tributaria, inversiones"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'tributaria_inversiones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de tributaria Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'tributaria_inversiones' debería activarse para 'Cuenta de tributaria Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de inversiones Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'tributaria_inversiones' debería activarse para 'Cuenta de inversiones Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_tributaria_inversiones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'tributaria_inversiones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_20901_imptos_match():
    """Debería activarse para cuentas con: 20901, imptos, diferidos, pasivo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '20901_imptos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 20901 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '20901_imptos' debería activarse para 'Cuenta de 20901 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de imptos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '20901_imptos' debería activarse para 'Cuenta de imptos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_20901_imptos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '20901_imptos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_alto_lote_match():
    """Debería activarse para cuentas con: alto, lote, nupangue"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'alto_lote'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de alto Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'alto_lote' debería activarse para 'Cuenta de alto Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de lote Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'alto_lote' debería activarse para 'Cuenta de lote Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_alto_lote_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'alto_lote'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_saldo_inicial_match():
    """Debería activarse para cuentas con: saldo, inicial"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'saldo_inicial'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de saldo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'saldo_inicial' debería activarse para 'Cuenta de saldo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de inicial Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'saldo_inicial' debería activarse para 'Cuenta de inicial Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_saldo_inicial_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'saldo_inicial'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_2020_activo_match():
    """Debería activarse para cuentas con: 2020, activo, fijo, ifrs"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '2020_activo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 2020 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '2020_activo' debería activarse para 'Cuenta de 2020 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de activo Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '2020_activo' debería activarse para 'Cuenta de activo Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_2020_activo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '2020_activo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_magdalena_diaz_match():
    """Debería activarse para cuentas con: magdalena, diaz, ramirez, plantacion"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'magdalena_diaz'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de magdalena Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'magdalena_diaz' debería activarse para 'Cuenta de magdalena Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de diaz Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'magdalena_diaz' debería activarse para 'Cuenta de diaz Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_magdalena_diaz_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'magdalena_diaz'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_agroinsumos_plantacion_match():
    """Debería activarse para cuentas con: agroinsumos, plantacion, fertilizante"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'agroinsumos_plantacion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de agroinsumos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'agroinsumos_plantacion' debería activarse para 'Cuenta de agroinsumos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de plantacion Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'agroinsumos_plantacion' debería activarse para 'Cuenta de plantacion Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_agroinsumos_plantacion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'agroinsumos_plantacion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_valdivieso_match():
    """Debería activarse para cuentas con: valdivieso"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'valdivieso'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de valdivieso Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'valdivieso' debería activarse para 'Cuenta de valdivieso Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0


def test_valdivieso_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'valdivieso'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_mendoza_2020_match():
    """Debería activarse para cuentas con: mendoza, 2020"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mendoza_2020'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de mendoza Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'mendoza_2020' debería activarse para 'Cuenta de mendoza Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 2020 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'mendoza_2020' debería activarse para 'Cuenta de 2020 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_mendoza_2020_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mendoza_2020'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_2020_match():
    """Debería activarse para cuentas con: 2020"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '2020'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 2020 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '2020' debería activarse para 'Cuenta de 2020 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0


def test_2020_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '2020'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_tarjeta_instituciones_match():
    """Debería activarse para cuentas con: tarjeta, instituciones, prevision"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'tarjeta_instituciones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de tarjeta Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'tarjeta_instituciones' debería activarse para 'Cuenta de tarjeta Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de instituciones Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'tarjeta_instituciones' debería activarse para 'Cuenta de instituciones Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_tarjeta_instituciones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'tarjeta_instituciones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_aporte_soc_match():
    """Debería activarse para cuentas con: aporte, soc, transp, libardom"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'aporte_soc'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de aporte Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'aporte_soc' debería activarse para 'Cuenta de aporte Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de soc Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'aporte_soc' debería activarse para 'Cuenta de soc Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_aporte_soc_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'aporte_soc'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_sanidad_ambiental_match():
    """Debería activarse para cuentas con: sanidad, ambiental, servicio, serv"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'sanidad_ambiental'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de sanidad Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'sanidad_ambiental' debería activarse para 'Cuenta de sanidad Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de ambiental Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'sanidad_ambiental' debería activarse para 'Cuenta de ambiental Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_sanidad_ambiental_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'sanidad_ambiental'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_cuentas_cobrar_match():
    """Debería activarse para cuentas con: cuentas, cobrar, pasivo, circulante"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cuentas_cobrar'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de cuentas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'cuentas_cobrar' debería activarse para 'Cuenta de cuentas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cobrar Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'cuentas_cobrar' debería activarse para 'Cuenta de cobrar Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_cuentas_cobrar_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cuentas_cobrar'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_imponible_capital_match():
    """Debería activarse para cuentas con: imponible, capital, propio, renta, liquida"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'imponible_capital'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de imponible Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'imponible_capital' debería activarse para 'Cuenta de imponible Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de capital Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'imponible_capital' debería activarse para 'Cuenta de capital Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_imponible_capital_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'imponible_capital'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_138_pagar_match():
    """Debería activarse para cuentas con: 138, pagar, impuestos, tarjeta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '138_pagar'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 138 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '138_pagar' debería activarse para 'Cuenta de 138 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de pagar Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '138_pagar' debería activarse para 'Cuenta de pagar Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_138_pagar_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '138_pagar'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_crecimiento_reguladores_match():
    """Debería activarse para cuentas con: crecimiento, reguladores, parrones"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'crecimiento_reguladores'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de crecimiento Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'crecimiento_reguladores' debería activarse para 'Cuenta de crecimiento Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de reguladores Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'crecimiento_reguladores' debería activarse para 'Cuenta de reguladores Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_crecimiento_reguladores_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'crecimiento_reguladores'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_patrimonio_financiero_match():
    """Debería activarse para cuentas con: patrimonio, financiero, capital, enterado, total"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'patrimonio_financiero'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de patrimonio Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'patrimonio_financiero' debería activarse para 'Cuenta de patrimonio Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de financiero Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'patrimonio_financiero' debería activarse para 'Cuenta de financiero Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_patrimonio_financiero_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'patrimonio_financiero'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_rentas_efectivas_match():
    """Debería activarse para cuentas con: rentas, efectivas, obre, fecha, vencimiento"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'rentas_efectivas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de rentas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'rentas_efectivas' debería activarse para 'Cuenta de rentas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de efectivas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'rentas_efectivas' debería activarse para 'Cuenta de efectivas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_rentas_efectivas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'rentas_efectivas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_estados_intermedios_match():
    """Debería activarse para cuentas con: estados, intermedios, financieros"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'estados_intermedios'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de estados Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'estados_intermedios' debería activarse para 'Cuenta de estados Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de intermedios Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'estados_intermedios' debería activarse para 'Cuenta de intermedios Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_estados_intermedios_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'estados_intermedios'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_asesorias_contables_match():
    """Debería activarse para cuentas con: asesorias, contables, 900, 4103"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'asesorias_contables'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de asesorias Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'asesorias_contables' debería activarse para 'Cuenta de asesorias Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de contables Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'asesorias_contables' debería activarse para 'Cuenta de contables Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_asesorias_contables_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'asesorias_contables'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_direccion_183_match():
    """Debería activarse para cuentas con: direccion, 183, avtajamar"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'direccion_183'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de direccion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'direccion_183' debería activarse para 'Cuenta de direccion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 183 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'direccion_183' debería activarse para 'Cuenta de 183 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_direccion_183_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'direccion_183'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_construcciones_reparac_match():
    """Debería activarse para cuentas con: construcciones, reparac, deprec, gener, habitac"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'construcciones_reparac'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de construcciones Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'construcciones_reparac' debería activarse para 'Cuenta de construcciones Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de reparac Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'construcciones_reparac' debería activarse para 'Cuenta de reparac Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_construcciones_reparac_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'construcciones_reparac'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_vehiculos_carga_match():
    """Debería activarse para cuentas con: vehiculos, carga, reparac"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'vehiculos_carga'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de vehiculos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'vehiculos_carga' debería activarse para 'Cuenta de vehiculos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de carga Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'vehiculos_carga' debería activarse para 'Cuenta de carga Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_vehiculos_carga_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'vehiculos_carga'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_fluctuaciones_cambio_match():
    """Debería activarse para cuentas con: fluctuaciones, cambio, desfar, favore"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'fluctuaciones_cambio'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de fluctuaciones Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'fluctuaciones_cambio' debería activarse para 'Cuenta de fluctuaciones Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cambio Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'fluctuaciones_cambio' debería activarse para 'Cuenta de cambio Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_fluctuaciones_cambio_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'fluctuaciones_cambio'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_aduanas_gastos_match():
    """Debería activarse para cuentas con: aduanas, gastos, agencias, 20084agencia"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'aduanas_gastos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de aduanas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'aduanas_gastos' debería activarse para 'Cuenta de aduanas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de gastos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'aduanas_gastos' debería activarse para 'Cuenta de gastos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_aduanas_gastos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'aduanas_gastos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_depositos_plazo_match():
    """Debería activarse para cuentas con: depositos, plazo, bco, santander"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'depositos_plazo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de depositos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'depositos_plazo' debería activarse para 'Cuenta de depositos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de plazo Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'depositos_plazo' debería activarse para 'Cuenta de plazo Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_depositos_plazo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'depositos_plazo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_iva_credito_match():
    """Debería activarse para cuentas con: iva, credito, incluido"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'iva_credito'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de iva Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'iva_credito' debería activarse para 'Cuenta de iva Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de credito Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'iva_credito' debería activarse para 'Cuenta de credito Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_iva_credito_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'iva_credito'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_agricolas_servicio_match():
    """Debería activarse para cuentas con: agricolas, servicio, cosecha, prestacion"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'agricolas_servicio'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de agricolas Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'agricolas_servicio' debería activarse para 'Cuenta de agricolas Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de servicio Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'agricolas_servicio' debería activarse para 'Cuenta de servicio Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_agricolas_servicio_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'agricolas_servicio'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_emitir_factura_match():
    """Debería activarse para cuentas con: emitir, factura, facturas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'emitir_factura'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de emitir Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'emitir_factura' debería activarse para 'Cuenta de emitir Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de factura Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'emitir_factura' debería activarse para 'Cuenta de factura Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_emitir_factura_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'emitir_factura'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_cta_cte_match():
    """Debería activarse para cuentas con: cta, cte, mival, arica, camila"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cta_cte'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de cta Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'cta_cte' debería activarse para 'Cuenta de cta Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cte Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'cta_cte' debería activarse para 'Cuenta de cte Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_cta_cte_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cta_cte'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_merchandising_30080venta_match():
    """Debería activarse para cuentas con: merchandising, 30080venta, 40090costo, venta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'merchandising_30080venta'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de merchandising Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'merchandising_30080venta' debería activarse para 'Cuenta de merchandising Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 30080venta Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'merchandising_30080venta' debería activarse para 'Cuenta de 30080venta Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_merchandising_30080venta_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'merchandising_30080venta'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_embotellado_30100venta_match():
    """Debería activarse para cuentas con: embotellado, 30100venta, 40110costo, vta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'embotellado_30100venta'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de embotellado Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'embotellado_30100venta' debería activarse para 'Cuenta de embotellado Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 30100venta Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'embotellado_30100venta' debería activarse para 'Cuenta de 30100venta Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_embotellado_30100venta_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'embotellado_30100venta'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_muestras_30120venta_match():
    """Debería activarse para cuentas con: muestras, 30120venta, 40130costo, venta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'muestras_30120venta'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de muestras Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'muestras_30120venta' debería activarse para 'Cuenta de muestras Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 30120venta Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'muestras_30120venta' debería activarse para 'Cuenta de 30120venta Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_muestras_30120venta_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'muestras_30120venta'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_produccion_eerr_match():
    """Debería activarse para cuentas con: produccion, eerr, 30271venta, insumos, 40371costo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'produccion_eerr'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de produccion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'produccion_eerr' debería activarse para 'Cuenta de produccion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de eerr Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'produccion_eerr' debería activarse para 'Cuenta de eerr Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_produccion_eerr_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'produccion_eerr'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_venta_40060costo_match():
    """Debería activarse para cuentas con: venta, 40060costo, tour, 40120costo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'venta_40060costo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de venta Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'venta_40060costo' debería activarse para 'Cuenta de venta Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 40060costo Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'venta_40060costo' debería activarse para 'Cuenta de 40060costo Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_venta_40060costo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'venta_40060costo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_banco_pagados_match():
    """Debería activarse para cuentas con: banco, pagados, 40191interes"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'banco_pagados'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de banco Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'banco_pagados' debería activarse para 'Cuenta de banco Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de pagados Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'banco_pagados' debería activarse para 'Cuenta de pagados Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_banco_pagados_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'banco_pagados'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_imponibles_pers_match():
    """Debería activarse para cuentas con: imponibles, pers, 50070otros, 50080otros"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'imponibles_pers'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de imponibles Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'imponibles_pers' debería activarse para 'Cuenta de imponibles Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de pers Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'imponibles_pers' debería activarse para 'Cuenta de pers Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_imponibles_pers_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'imponibles_pers'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_viajes_50370gastos_match():
    """Debería activarse para cuentas con: viajes, 50370gastos, 42129, gastos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'viajes_50370gastos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de viajes Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'viajes_50370gastos' debería activarse para 'Cuenta de viajes Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 50370gastos Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'viajes_50370gastos' debería activarse para 'Cuenta de 50370gastos Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_viajes_50370gastos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'viajes_50370gastos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_gastos_telefonos_match():
    """Debería activarse para cuentas con: gastos, telefonos, 10127, 9227"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_telefonos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de gastos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'gastos_telefonos' debería activarse para 'Cuenta de gastos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de telefonos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'gastos_telefonos' debería activarse para 'Cuenta de telefonos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_gastos_telefonos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_telefonos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_pag_match():
    """Debería activarse para cuentas con: pag"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pag'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de pag Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'pag' debería activarse para 'Cuenta de pag Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0


def test_pag_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pag'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_compra_prov_match():
    """Debería activarse para cuentas con: compra, prov, fruta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'compra_prov'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de compra Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'compra_prov' debería activarse para 'Cuenta de compra Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de prov Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'compra_prov' debería activarse para 'Cuenta de prov Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_compra_prov_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'compra_prov'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_patentes_2301974_match():
    """Debería activarse para cuentas con: patentes, 2301974"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'patentes_2301974'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de patentes Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'patentes_2301974' debería activarse para 'Cuenta de patentes Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 2301974 Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'patentes_2301974' debería activarse para 'Cuenta de 2301974 Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_patentes_2301974_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'patentes_2301974'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_utilidad_perdida_match():
    """Debería activarse para cuentas con: utilidad, perdida, ingresos, explotac"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'utilidad_perdida'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de utilidad Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'utilidad_perdida' debería activarse para 'Cuenta de utilidad Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de perdida Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'utilidad_perdida' debería activarse para 'Cuenta de perdida Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_utilidad_perdida_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'utilidad_perdida'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_valores_negociables_match():
    """Debería activarse para cuentas con: valores, negociables, cuentas, pagar"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'valores_negociables'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de valores Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'valores_negociables' debería activarse para 'Cuenta de valores Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de negociables Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'valores_negociables' debería activarse para 'Cuenta de negociables Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_valores_negociables_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'valores_negociables'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_otros_activos_match():
    """Debería activarse para cuentas con: otros, activos, circulantes, corr, mon"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'otros_activos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de otros Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'otros_activos' debería activarse para 'Cuenta de otros Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de activos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'otros_activos' debería activarse para 'Cuenta de activos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_otros_activos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'otros_activos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_rut_match():
    """Debería activarse para cuentas con: rut"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'rut'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de rut Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'rut' debería activarse para 'Cuenta de rut Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0


def test_rut_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'rut'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_patrimonio_atribuible_match():
    """Debería activarse para cuentas con: patrimonio, atribuible, propietarios"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'patrimonio_atribuible'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de patrimonio Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'patrimonio_atribuible' debería activarse para 'Cuenta de patrimonio Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de atribuible Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'patrimonio_atribuible' debería activarse para 'Cuenta de atribuible Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_patrimonio_atribuible_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'patrimonio_atribuible'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_pagar_clientes_match():
    """Debería activarse para cuentas con: pagar, clientes, campomanes, cuentas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pagar_clientes'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de pagar Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'pagar_clientes' debería activarse para 'Cuenta de pagar Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de clientes Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'pagar_clientes' debería activarse para 'Cuenta de clientes Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_pagar_clientes_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pagar_clientes'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_clientes_acreedores_match():
    """Debería activarse para cuentas con: clientes, acreedores, varios, casa, garcia"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'clientes_acreedores'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de clientes Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'clientes_acreedores' debería activarse para 'Cuenta de clientes Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de acreedores Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'clientes_acreedores' debería activarse para 'Cuenta de acreedores Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_clientes_acreedores_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'clientes_acreedores'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_sueldos_clientes_match():
    """Debería activarse para cuentas con: sueldos, clientes, pagar, asturias"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'sueldos_clientes'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de sueldos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'sueldos_clientes' debería activarse para 'Cuenta de sueldos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de clientes Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'sueldos_clientes' debería activarse para 'Cuenta de clientes Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_sueldos_clientes_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'sueldos_clientes'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_proveedores_regularizar_match():
    """Debería activarse para cuentas con: proveedores, regularizar, 161, cta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'proveedores_regularizar'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de proveedores Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'proveedores_regularizar' debería activarse para 'Cuenta de proveedores Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de regularizar Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'proveedores_regularizar' debería activarse para 'Cuenta de regularizar Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_proveedores_regularizar_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'proveedores_regularizar'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_cuentas_pagar_match():
    """Debería activarse para cuentas con: cuentas, pagar, tarjeta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cuentas_pagar'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de cuentas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'cuentas_pagar' debería activarse para 'Cuenta de cuentas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de pagar Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'cuentas_pagar' debería activarse para 'Cuenta de pagar Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_cuentas_pagar_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cuentas_pagar'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_pons_garcia_match():
    """Debería activarse para cuentas con: pons, garcia, part, maria, dol"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pons_garcia'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de pons Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'pons_garcia' debería activarse para 'Cuenta de pons Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de garcia Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'pons_garcia' debería activarse para 'Cuenta de garcia Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_pons_garcia_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pons_garcia'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_depreciacion_financiera_match():
    """Debería activarse para cuentas con: depreciacion, financiera"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'depreciacion_financiera'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de depreciacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'depreciacion_financiera' debería activarse para 'Cuenta de depreciacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de financiera Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'depreciacion_financiera' debería activarse para 'Cuenta de financiera Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_depreciacion_financiera_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'depreciacion_financiera'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_usufructo_match():
    """Debería activarse para cuentas con: usufructo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'usufructo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de usufructo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'usufructo' debería activarse para 'Cuenta de usufructo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0


def test_usufructo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'usufructo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_cta_cte_match():
    """Debería activarse para cuentas con: cta, cte, personal, 11305"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cta_cte'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de cta Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'cta_cte' debería activarse para 'Cuenta de cta Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cte Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'cta_cte' debería activarse para 'Cuenta de cte Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_cta_cte_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cta_cte'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_gastos_operacion_match():
    """Debería activarse para cuentas con: gastos, operacion, factoring, 50701, 9999"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_operacion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de gastos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'gastos_operacion' debería activarse para 'Cuenta de gastos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de operacion Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'gastos_operacion' debería activarse para 'Cuenta de operacion Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_gastos_operacion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_operacion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_contabilidad_saldo_match():
    """Debería activarse para cuentas con: contabilidad, saldo, segun"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'contabilidad_saldo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de contabilidad Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'contabilidad_saldo' debería activarse para 'Cuenta de contabilidad Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de saldo Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'contabilidad_saldo' debería activarse para 'Cuenta de saldo Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_contabilidad_saldo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'contabilidad_saldo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_pablo_match():
    """Debería activarse para cuentas con: pablo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pablo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de pablo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'pablo' debería activarse para 'Cuenta de pablo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0


def test_pablo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pablo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_mauricio_ramirez_match():
    """Debería activarse para cuentas con: mauricio, ramirez, rendir"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mauricio_ramirez'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de mauricio Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'mauricio_ramirez' debería activarse para 'Cuenta de mauricio Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de ramirez Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'mauricio_ramirez' debería activarse para 'Cuenta de ramirez Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_mauricio_ramirez_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mauricio_ramirez'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_san_12103_match():
    """Debería activarse para cuentas con: san, 12103, ramon, pencahue"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'san_12103'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de san Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'san_12103' debería activarse para 'Cuenta de san Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 12103 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'san_12103' debería activarse para 'Cuenta de 12103 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_san_12103_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'san_12103'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_12108_san_match():
    """Debería activarse para cuentas con: 12108, san, luis"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '12108_san'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 12108 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '12108_san' debería activarse para 'Cuenta de 12108 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de san Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '12108_san' debería activarse para 'Cuenta de san Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_12108_san_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '12108_san'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_cipres_12139_match():
    """Debería activarse para cuentas con: cipres, 12139"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cipres_12139'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de cipres Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'cipres_12139' debería activarse para 'Cuenta de cipres Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 12139 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'cipres_12139' debería activarse para 'Cuenta de 12139 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_cipres_12139_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cipres_12139'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_compras_activadas_match():
    """Debería activarse para cuentas con: compras, activadas, compra, activo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'compras_activadas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de compras Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'compras_activadas' debería activarse para 'Cuenta de compras Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de activadas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'compras_activadas' debería activarse para 'Cuenta de activadas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_compras_activadas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'compras_activadas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_plantacion_tutores_match():
    """Debería activarse para cuentas con: plantacion, tutores, alambre"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_tutores'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de plantacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'plantacion_tutores' debería activarse para 'Cuenta de plantacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de tutores Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'plantacion_tutores' debería activarse para 'Cuenta de tutores Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_plantacion_tutores_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_tutores'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_ltda_plantacion_match():
    """Debería activarse para cuentas con: ltda, plantacion, tutores, torres, miranda"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ltda_plantacion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de ltda Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'ltda_plantacion' debería activarse para 'Cuenta de ltda Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de plantacion Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'ltda_plantacion' debería activarse para 'Cuenta de plantacion Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_ltda_plantacion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ltda_plantacion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_meses_amortizar_match():
    """Debería activarse para cuentas con: meses, amortizar, contar, fecha"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'meses_amortizar'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de meses Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'meses_amortizar' debería activarse para 'Cuenta de meses Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de amortizar Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'meses_amortizar' debería activarse para 'Cuenta de amortizar Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_meses_amortizar_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'meses_amortizar'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_2020_match():
    """Debería activarse para cuentas con: 2020"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '2020'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 2020 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '2020' debería activarse para 'Cuenta de 2020 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0


def test_2020_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '2020'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_utilidades_acumuladas_match():
    """Debería activarse para cuentas con: utilidades, acumuladas, 23302, saldo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'utilidades_acumuladas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de utilidades Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'utilidades_acumuladas' debería activarse para 'Cuenta de utilidades Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de acumuladas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'utilidades_acumuladas' debería activarse para 'Cuenta de acumuladas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_utilidades_acumuladas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'utilidades_acumuladas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_retiro_utilidades_match():
    """Debería activarse para cuentas con: retiro, utilidades, inmob, lobarnechea"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'retiro_utilidades'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de retiro Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'retiro_utilidades' debería activarse para 'Cuenta de retiro Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de utilidades Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'retiro_utilidades' debería activarse para 'Cuenta de utilidades Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_retiro_utilidades_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'retiro_utilidades'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_ppua_ajuste_match():
    """Debería activarse para cuentas con: ppua, ajuste, at2014"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ppua_ajuste'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de ppua Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'ppua_ajuste' debería activarse para 'Cuenta de ppua Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de ajuste Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'ppua_ajuste' debería activarse para 'Cuenta de ajuste Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_ppua_ajuste_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ppua_ajuste'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_2006_juan_match():
    """Debería activarse para cuentas con: 2006, juan, maria"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '2006_juan'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 2006 Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '2006_juan' debería activarse para 'Cuenta de 2006 Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de juan Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '2006_juan' debería activarse para 'Cuenta de juan Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_2006_juan_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '2006_juan'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_mercaderias_pasivo_match():
    """Debería activarse para cuentas con: mercaderias, pasivo, circulante, 327, 082"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mercaderias_pasivo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de mercaderias Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'mercaderias_pasivo' debería activarse para 'Cuenta de mercaderias Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de pasivo Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'mercaderias_pasivo' debería activarse para 'Cuenta de pasivo Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_mercaderias_pasivo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mercaderias_pasivo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_impto_renta_match():
    """Debería activarse para cuentas con: impto, renta, art, at2019"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'impto_renta'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de impto Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'impto_renta' debería activarse para 'Cuenta de impto Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de renta Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'impto_renta' debería activarse para 'Cuenta de renta Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_impto_renta_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'impto_renta'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_renta_impto_match():
    """Debería activarse para cuentas con: renta, impto"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'renta_impto'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de renta Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'renta_impto' debería activarse para 'Cuenta de renta Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de impto Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'renta_impto' debería activarse para 'Cuenta de impto Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_renta_impto_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'renta_impto'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_eventos_especiales_match():
    """Debería activarse para cuentas con: eventos, especiales"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'eventos_especiales'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de eventos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'eventos_especiales' debería activarse para 'Cuenta de eventos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de especiales Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'eventos_especiales' debería activarse para 'Cuenta de especiales Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_eventos_especiales_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'eventos_especiales'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_productos_quimicos_match():
    """Debería activarse para cuentas con: productos, quimicos, otros, costo, 206"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'productos_quimicos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de productos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'productos_quimicos' debería activarse para 'Cuenta de productos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de quimicos Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'productos_quimicos' debería activarse para 'Cuenta de quimicos Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_productos_quimicos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'productos_quimicos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_frigorifico_obras_match():
    """Debería activarse para cuentas con: frigorifico, obras"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'frigorifico_obras'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de frigorifico Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'frigorifico_obras' debería activarse para 'Cuenta de frigorifico Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de obras Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'frigorifico_obras' debería activarse para 'Cuenta de obras Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_frigorifico_obras_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'frigorifico_obras'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_dep_acum_match():
    """Debería activarse para cuentas con: dep, acum, superior, software"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'dep_acum'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de dep Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'dep_acum' debería activarse para 'Cuenta de dep Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de acum Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'dep_acum' debería activarse para 'Cuenta de acum Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_dep_acum_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'dep_acum'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_uvas_usas_match():
    """Debería activarse para cuentas con: uvas, usas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'uvas_usas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de uvas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'uvas_usas' debería activarse para 'Cuenta de uvas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de usas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'uvas_usas' debería activarse para 'Cuenta de usas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_uvas_usas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'uvas_usas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_recuperar_impuestos_match():
    """Debería activarse para cuentas con: recuperar, impuestos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'recuperar_impuestos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de recuperar Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'recuperar_impuestos' debería activarse para 'Cuenta de recuperar Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de impuestos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'recuperar_impuestos' debería activarse para 'Cuenta de impuestos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_recuperar_impuestos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'recuperar_impuestos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_otros_activos_match():
    """Debería activarse para cuentas con: otros, activos, largo, plazo, mon"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'otros_activos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de otros Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'otros_activos' debería activarse para 'Cuenta de otros Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de activos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'otros_activos' debería activarse para 'Cuenta de activos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_otros_activos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'otros_activos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_vencimiento_provisionales_match():
    """Debería activarse para cuentas con: vencimiento, provisionales, art, fecha"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'vencimiento_provisionales'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de vencimiento Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'vencimiento_provisionales' debería activarse para 'Cuenta de vencimiento Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de provisionales Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'vencimiento_provisionales' debería activarse para 'Cuenta de provisionales Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_vencimiento_provisionales_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'vencimiento_provisionales'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_art_renta_match():
    """Debería activarse para cuentas con: art, renta, liquida, perdida"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'art_renta'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de art Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'art_renta' debería activarse para 'Cuenta de art Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de renta Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'art_renta' debería activarse para 'Cuenta de renta Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_art_renta_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'art_renta'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_mercaderias_importadas_match():
    """Debería activarse para cuentas con: mercaderias, importadas, 173, 670, 519"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mercaderias_importadas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de mercaderias Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'mercaderias_importadas' debería activarse para 'Cuenta de mercaderias Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de importadas Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'mercaderias_importadas' debería activarse para 'Cuenta de importadas Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_mercaderias_importadas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mercaderias_importadas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_cuentas_largo_match():
    """Debería activarse para cuentas con: cuentas, largo, plazo, pago"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cuentas_largo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de cuentas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'cuentas_largo' debería activarse para 'Cuenta de cuentas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de largo Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'cuentas_largo' debería activarse para 'Cuenta de largo Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_cuentas_largo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cuentas_largo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_tarjeta_sueldos_match():
    """Debería activarse para cuentas con: tarjeta, sueldos, pagar"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'tarjeta_sueldos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de tarjeta Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'tarjeta_sueldos' debería activarse para 'Cuenta de tarjeta Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de sueldos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'tarjeta_sueldos' debería activarse para 'Cuenta de sueldos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_tarjeta_sueldos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'tarjeta_sueldos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_cuentas_cobrar_match():
    """Debería activarse para cuentas con: cuentas, cobrar, empresa, relac"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cuentas_cobrar'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de cuentas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'cuentas_cobrar' debería activarse para 'Cuenta de cuentas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cobrar Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'cuentas_cobrar' debería activarse para 'Cuenta de cobrar Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_cuentas_cobrar_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cuentas_cobrar'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_tipo_perdida_match():
    """Debería activarse para cuentas con: tipo, perdida, camblo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'tipo_perdida'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de tipo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'tipo_perdida' debería activarse para 'Cuenta de tipo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de perdida Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'tipo_perdida' debería activarse para 'Cuenta de perdida Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_tipo_perdida_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'tipo_perdida'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_derechos_concesiones_match():
    """Debería activarse para cuentas con: derechos, concesiones, suscripciones"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'derechos_concesiones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de derechos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'derechos_concesiones' debería activarse para 'Cuenta de derechos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de concesiones Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'derechos_concesiones' debería activarse para 'Cuenta de concesiones Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_derechos_concesiones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'derechos_concesiones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_activo_impuestos_match():
    """Debería activarse para cuentas con: activo, impuestos, corrientes, total"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'activo_impuestos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de activo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'activo_impuestos' debería activarse para 'Cuenta de activo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de impuestos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'activo_impuestos' debería activarse para 'Cuenta de impuestos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_activo_impuestos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'activo_impuestos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_margen_bruto_match():
    """Debería activarse para cuentas con: margen, bruto"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'margen_bruto'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de margen Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'margen_bruto' debería activarse para 'Cuenta de margen Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de bruto Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'margen_bruto' debería activarse para 'Cuenta de bruto Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_margen_bruto_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'margen_bruto'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_inversiones_philadelphia_match():
    """Debería activarse para cuentas con: inversiones, philadelphia, spa, ibiza"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'inversiones_philadelphia'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de inversiones Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'inversiones_philadelphia' debería activarse para 'Cuenta de inversiones Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de philadelphia Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'inversiones_philadelphia' debería activarse para 'Cuenta de philadelphia Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_inversiones_philadelphia_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'inversiones_philadelphia'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_provisional_cat_match():
    """Debería activarse para cuentas con: provisional, cat, impto, 11199994"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'provisional_cat'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de provisional Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'provisional_cat' debería activarse para 'Cuenta de provisional Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cat Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'provisional_cat' debería activarse para 'Cuenta de cat Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_provisional_cat_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'provisional_cat'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_riego_mecanizado_match():
    """Debería activarse para cuentas con: riego, mecanizado, 3715414, 412824, 3302598"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'riego_mecanizado'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de riego Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'riego_mecanizado' debería activarse para 'Cuenta de riego Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de mecanizado Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'riego_mecanizado' debería activarse para 'Cuenta de mecanizado Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_riego_mecanizado_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'riego_mecanizado'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_familiar_asig_match():
    """Debería activarse para cuentas con: familiar, asig, asignacion, 200141"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'familiar_asig'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de familiar Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'familiar_asig' debería activarse para 'Cuenta de familiar Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de asig Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'familiar_asig' debería activarse para 'Cuenta de asig Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_familiar_asig_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'familiar_asig'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_chile_spa_match():
    """Debería activarse para cuentas con: chile, spa, pagina, capiro, captro"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'chile_spa'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de chile Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'chile_spa' debería activarse para 'Cuenta de chile Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de spa Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'chile_spa' debería activarse para 'Cuenta de spa Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_chile_spa_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'chile_spa'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_costos_4101_match():
    """Debería activarse para cuentas con: costos, 4101"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'costos_4101'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de costos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'costos_4101' debería activarse para 'Cuenta de costos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 4101 Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'costos_4101' debería activarse para 'Cuenta de 4101 Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_costos_4101_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'costos_4101'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_ajuste_centratizacion_match():
    """Debería activarse para cuentas con: ajuste, centratizacion, centralizacion"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ajuste_centratizacion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de ajuste Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'ajuste_centratizacion' debería activarse para 'Cuenta de ajuste Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de centratizacion Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'ajuste_centratizacion' debería activarse para 'Cuenta de centratizacion Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_ajuste_centratizacion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ajuste_centratizacion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_fabrica_dest_match():
    """Debería activarse para cuentas con: fabrica, dest, etilico, comercial, agroindustrial"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'fabrica_dest'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de fabrica Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'fabrica_dest' debería activarse para 'Cuenta de fabrica Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de dest Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'fabrica_dest' debería activarse para 'Cuenta de dest Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_fabrica_dest_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'fabrica_dest'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_arriendo_maq_match():
    """Debería activarse para cuentas con: arriendo, maq, vehiculos, vehiculo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'arriendo_maq'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de arriendo Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'arriendo_maq' debería activarse para 'Cuenta de arriendo Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de maq Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'arriendo_maq' debería activarse para 'Cuenta de maq Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_arriendo_maq_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'arriendo_maq'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_desdeenero_2018_match():
    """Debería activarse para cuentas con: desdeenero, 2018, hastadiciembre"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'desdeenero_2018'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de desdeenero Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'desdeenero_2018' debería activarse para 'Cuenta de desdeenero Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 2018 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'desdeenero_2018' debería activarse para 'Cuenta de 2018 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_desdeenero_2018_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'desdeenero_2018'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_produccion_gastos_match():
    """Debería activarse para cuentas con: produccion, gastos, general, costo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'produccion_gastos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de produccion Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'produccion_gastos' debería activarse para 'Cuenta de produccion Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de gastos Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'produccion_gastos' debería activarse para 'Cuenta de gastos Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_produccion_gastos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'produccion_gastos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_venta_uva_match():
    """Debería activarse para cuentas con: venta, uva, mesa, pais"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'venta_uva'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de venta Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'venta_uva' debería activarse para 'Cuenta de venta Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de uva Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'venta_uva' debería activarse para 'Cuenta de uva Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_venta_uva_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'venta_uva'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_gtos_vtas_match():
    """Debería activarse para cuentas con: gtos, vtas, comision, sodexo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gtos_vtas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de gtos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'gtos_vtas' debería activarse para 'Cuenta de gtos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de vtas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'gtos_vtas' debería activarse para 'Cuenta de vtas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_gtos_vtas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gtos_vtas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_asesorias_externas_match():
    """Debería activarse para cuentas con: asesorias, externas, operacion"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'asesorias_externas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de asesorias Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'asesorias_externas' debería activarse para 'Cuenta de asesorias Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de externas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'asesorias_externas' debería activarse para 'Cuenta de externas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_asesorias_externas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'asesorias_externas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_uniformes_implementos_match():
    """Debería activarse para cuentas con: uniformes, implementos, tra"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'uniformes_implementos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de uniformes Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'uniformes_implementos' debería activarse para 'Cuenta de uniformes Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de implementos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'uniformes_implementos' debería activarse para 'Cuenta de implementos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_uniformes_implementos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'uniformes_implementos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_indemnizaciones_desahucios_match():
    """Debería activarse para cuentas con: indemnizaciones, desahucios"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'indemnizaciones_desahucios'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de indemnizaciones Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'indemnizaciones_desahucios' debería activarse para 'Cuenta de indemnizaciones Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de desahucios Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'indemnizaciones_desahucios' debería activarse para 'Cuenta de desahucios Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_indemnizaciones_desahucios_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'indemnizaciones_desahucios'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_fletes_reparto_match():
    """Debería activarse para cuentas con: fletes, reparto"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'fletes_reparto'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de fletes Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'fletes_reparto' debería activarse para 'Cuenta de fletes Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de reparto Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'fletes_reparto' debería activarse para 'Cuenta de reparto Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_fletes_reparto_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'fletes_reparto'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_gastos_seguridad_match():
    """Debería activarse para cuentas con: gastos, seguridad, alarmas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_seguridad'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de gastos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'gastos_seguridad' debería activarse para 'Cuenta de gastos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de seguridad Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'gastos_seguridad' debería activarse para 'Cuenta de seguridad Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_gastos_seguridad_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_seguridad'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_registro_gastos_match():
    """Debería activarse para cuentas con: registro, gastos, notaria, publicacion"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'registro_gastos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de registro Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'registro_gastos' debería activarse para 'Cuenta de registro Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de gastos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'registro_gastos' debería activarse para 'Cuenta de gastos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_registro_gastos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'registro_gastos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_avena_siembra_match():
    """Debería activarse para cuentas con: avena, siembra"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'avena_siembra'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de avena Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'avena_siembra' debería activarse para 'Cuenta de avena Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de siembra Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'avena_siembra' debería activarse para 'Cuenta de siembra Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_avena_siembra_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'avena_siembra'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_almuerzos_40080costo_match():
    """Debería activarse para cuentas con: almuerzos, 40080costo, venta, 30070venta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'almuerzos_40080costo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de almuerzos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'almuerzos_40080costo' debería activarse para 'Cuenta de almuerzos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 40080costo Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'almuerzos_40080costo' debería activarse para 'Cuenta de 40080costo Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_almuerzos_40080costo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'almuerzos_40080costo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_renta_40480impuesto_match():
    """Debería activarse para cuentas con: renta, 40480impuesto, 40490impuesto"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'renta_40480impuesto'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de renta Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'renta_40480impuesto' debería activarse para 'Cuenta de renta Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 40480impuesto Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'renta_40480impuesto' debería activarse para 'Cuenta de 40480impuesto Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_renta_40480impuesto_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'renta_40480impuesto'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50610depreciaciones_50611depreciaciones_match():
    """Debería activarse para cuentas con: 50610depreciaciones, 50611depreciaciones, mvrt"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50610depreciaciones_50611depreciaciones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50610depreciaciones Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50610depreciaciones_50611depreciaciones' debería activarse para 'Cuenta de 50610depreciaciones Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 50611depreciaciones Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '50610depreciaciones_50611depreciaciones' debería activarse para 'Cuenta de 50611depreciaciones Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_50610depreciaciones_50611depreciaciones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50610depreciaciones_50611depreciaciones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_serbmit_otseupmi_match():
    """Debería activarse para cuentas con: serbmit, otseupmi"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'serbmit_otseupmi'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de serbmit Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'serbmit_otseupmi' debería activarse para 'Cuenta de serbmit Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de otseupmi Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'serbmit_otseupmi' debería activarse para 'Cuenta de otseupmi Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_serbmit_otseupmi_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'serbmit_otseupmi'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_retencion_impuestos_match():
    """Debería activarse para cuentas con: retencion, impuestos, 192428, 207983"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'retencion_impuestos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de retencion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'retencion_impuestos' debería activarse para 'Cuenta de retencion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de impuestos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'retencion_impuestos' debería activarse para 'Cuenta de impuestos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_retencion_impuestos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'retencion_impuestos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_2211_doom_match():
    """Debería activarse para cuentas con: 2211, doom"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '2211_doom'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 2211 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '2211_doom' debería activarse para 'Cuenta de 2211 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de doom Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '2211_doom' debería activarse para 'Cuenta de doom Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_2211_doom_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '2211_doom'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_anticipo_proveedores_match():
    """Debería activarse para cuentas con: anticipo, proveedores, 348"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'anticipo_proveedores'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de anticipo Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'anticipo_proveedores' debería activarse para 'Cuenta de anticipo Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de proveedores Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'anticipo_proveedores' debería activarse para 'Cuenta de proveedores Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_anticipo_proveedores_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'anticipo_proveedores'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_obras_inmueble_match():
    """Debería activarse para cuentas con: obras, inmueble"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'obras_inmueble'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de obras Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'obras_inmueble' debería activarse para 'Cuenta de obras Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de inmueble Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'obras_inmueble' debería activarse para 'Cuenta de inmueble Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_obras_inmueble_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'obras_inmueble'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_maquinaria_211376247_match():
    """Debería activarse para cuentas con: maquinaria, 211376247, 379, 018"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'maquinaria_211376247'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de maquinaria Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'maquinaria_211376247' debería activarse para 'Cuenta de maquinaria Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 211376247 Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'maquinaria_211376247' debería activarse para 'Cuenta de 211376247 Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_maquinaria_211376247_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'maquinaria_211376247'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_contratista_339_match():
    """Debería activarse para cuentas con: contratista, 339, 294, 130"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'contratista_339'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de contratista Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'contratista_339' debería activarse para 'Cuenta de contratista Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 339 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'contratista_339' debería activarse para 'Cuenta de 339 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_contratista_339_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'contratista_339'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_fumigaciones_8321_match():
    """Debería activarse para cuentas con: fumigaciones, 8321"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'fumigaciones_8321'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de fumigaciones Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'fumigaciones_8321' debería activarse para 'Cuenta de fumigaciones Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 8321 Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'fumigaciones_8321' debería activarse para 'Cuenta de 8321 Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_fumigaciones_8321_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'fumigaciones_8321'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_remuneraciones_122_match():
    """Debería activarse para cuentas con: remuneraciones, 122, 043"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'remuneraciones_122'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de remuneraciones Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'remuneraciones_122' debería activarse para 'Cuenta de remuneraciones Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 122 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'remuneraciones_122' debería activarse para 'Cuenta de 122 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_remuneraciones_122_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'remuneraciones_122'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_gastos_adm_match():
    """Debería activarse para cuentas con: gastos, adm, vtas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_adm'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de gastos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'gastos_adm' debería activarse para 'Cuenta de gastos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de adm Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'gastos_adm' debería activarse para 'Cuenta de adm Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_gastos_adm_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_adm'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_predio_tranque_match():
    """Debería activarse para cuentas con: predio, tranque"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'predio_tranque'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de predio Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'predio_tranque' debería activarse para 'Cuenta de predio Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de tranque Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'predio_tranque' debería activarse para 'Cuenta de tranque Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_predio_tranque_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'predio_tranque'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_total_pasivos_match():
    """Debería activarse para cuentas con: total, pasivos, puerto"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'total_pasivos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de total Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'total_pasivos' debería activarse para 'Cuenta de total Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de pasivos Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'total_pasivos' debería activarse para 'Cuenta de pasivos Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_total_pasivos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'total_pasivos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_vessels_and_match():
    """Debería activarse para cuentas con: vessels, and, machineries, paid, capital"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'vessels_and'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de vessels Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'vessels_and' debería activarse para 'Cuenta de vessels Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de and Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'vessels_and' debería activarse para 'Cuenta de and Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_vessels_and_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'vessels_and'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_austral_law_match():
    """Debería activarse para cuentas con: austral, law, retained, earnings"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'austral_law'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de austral Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'austral_law' debería activarse para 'Cuenta de austral Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de law Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'austral_law' debería activarse para 'Cuenta de law Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_austral_law_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'austral_law'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_net_income_match():
    """Debería activarse para cuentas con: net, income, for, the, period"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'net_income'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de net Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'net_income' debería activarse para 'Cuenta de net Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de income Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'net_income' debería activarse para 'Cuenta de income Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_net_income_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'net_income'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_long_term_match():
    """Debería activarse para cuentas con: long, term, inventory, 356, 733"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'long_term'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de long Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'long_term' debería activarse para 'Cuenta de long Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de term Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'long_term' debería activarse para 'Cuenta de term Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_long_term_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'long_term'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_leasing_payables_match():
    """Debería activarse para cuentas con: leasing, payables, tax, assets, current"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'leasing_payables'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de leasing Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'leasing_payables' debería activarse para 'Cuenta de leasing Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de payables Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'leasing_payables' debería activarse para 'Cuenta de payables Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_leasing_payables_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'leasing_payables'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_accounts_related_match():
    """Debería activarse para cuentas con: accounts, related, companies, insurance"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'accounts_related'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de accounts Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'accounts_related' debería activarse para 'Cuenta de accounts Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de related Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'accounts_related' debería activarse para 'Cuenta de related Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_accounts_related_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'accounts_related'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_payable_tax_match():
    """Debería activarse para cuentas con: payable, tax"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'payable_tax'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de payable Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'payable_tax' debería activarse para 'Cuenta de payable Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de tax Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'payable_tax' debería activarse para 'Cuenta de tax Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_payable_tax_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'payable_tax'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_witholdings_match():
    """Debería activarse para cuentas con: witholdings"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'witholdings'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de witholdings Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'witholdings' debería activarse para 'Cuenta de witholdings Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0


def test_witholdings_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'witholdings'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_clientes_santina_match():
    """Debería activarse para cuentas con: clientes, santina, 570, honorarios"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'clientes_santina'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de clientes Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'clientes_santina' debería activarse para 'Cuenta de clientes Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de santina Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'clientes_santina' debería activarse para 'Cuenta de santina Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_clientes_santina_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'clientes_santina'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_tarjeta_acreedores_match():
    """Debería activarse para cuentas con: tarjeta, acreedores, varios, credito"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'tarjeta_acreedores'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de tarjeta Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'tarjeta_acreedores' debería activarse para 'Cuenta de tarjeta Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de acreedores Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'tarjeta_acreedores' debería activarse para 'Cuenta de acreedores Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_tarjeta_acreedores_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'tarjeta_acreedores'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_anticipo_remuneraciones_match():
    """Debería activarse para cuentas con: anticipo, remuneraciones, 544, 288, iva"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'anticipo_remuneraciones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de anticipo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'anticipo_remuneraciones' debería activarse para 'Cuenta de anticipo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de remuneraciones Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'anticipo_remuneraciones' debería activarse para 'Cuenta de remuneraciones Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_anticipo_remuneraciones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'anticipo_remuneraciones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_saldo_segun_match():
    """Debería activarse para cuentas con: saldo, segun, cartola"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'saldo_segun'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de saldo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'saldo_segun' debería activarse para 'Cuenta de saldo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de segun Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'saldo_segun' debería activarse para 'Cuenta de segun Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_saldo_segun_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'saldo_segun'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_mensual_intereses_match():
    """Debería activarse para cuentas con: mensual, intereses, 505, 425"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mensual_intereses'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de mensual Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'mensual_intereses' debería activarse para 'Cuenta de mensual Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de intereses Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'mensual_intereses' debería activarse para 'Cuenta de intereses Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_mensual_intereses_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mensual_intereses'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_victor_padilla_match():
    """Debería activarse para cuentas con: victor, padilla"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'victor_padilla'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de victor Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'victor_padilla' debería activarse para 'Cuenta de victor Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de padilla Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'victor_padilla' debería activarse para 'Cuenta de padilla Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_victor_padilla_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'victor_padilla'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_francisco_750_match():
    """Debería activarse para cuentas con: francisco, 750"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'francisco_750'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de francisco Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'francisco_750' debería activarse para 'Cuenta de francisco Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 750 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'francisco_750' debería activarse para 'Cuenta de 750 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_francisco_750_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'francisco_750'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_56301_9999_match():
    """Debería activarse para cuentas con: 56301, 9999, 2020, contabiliza, impto"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '56301_9999'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 56301 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '56301_9999' debería activarse para 'Cuenta de 56301 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 9999 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '56301_9999' debería activarse para 'Cuenta de 9999 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_56301_9999_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '56301_9999'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_monta_12106_match():
    """Debería activarse para cuentas con: monta, 12106, mal"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'monta_12106'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de monta Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'monta_12106' debería activarse para 'Cuenta de monta Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 12106 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'monta_12106' debería activarse para 'Cuenta de 12106 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_monta_12106_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'monta_12106'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_san_12109_match():
    """Debería activarse para cuentas con: san, 12109, pedro"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'san_12109'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de san Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'san_12109' debería activarse para 'Cuenta de san Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 12109 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'san_12109' debería activarse para 'Cuenta de 12109 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_san_12109_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'san_12109'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_reclasificaciones_depresicaiones_match():
    """Debería activarse para cuentas con: reclasificaciones, depresicaiones"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'reclasificaciones_depresicaiones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de reclasificaciones Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'reclasificaciones_depresicaiones' debería activarse para 'Cuenta de reclasificaciones Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de depresicaiones Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'reclasificaciones_depresicaiones' debería activarse para 'Cuenta de depresicaiones Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_reclasificaciones_depresicaiones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'reclasificaciones_depresicaiones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_434_match():
    """Debería activarse para cuentas con: 434"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '434'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 434 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '434' debería activarse para 'Cuenta de 434 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0


def test_434_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '434'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_protector_prados_match():
    """Debería activarse para cuentas con: protector, prados, ltda, plantacion"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'protector_prados'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de protector Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'protector_prados' debería activarse para 'Cuenta de protector Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de prados Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'protector_prados' debería activarse para 'Cuenta de prados Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_protector_prados_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'protector_prados'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_plantas_gallardo_match():
    """Debería activarse para cuentas con: plantas, gallardo, salgado, plantacion, 79hector"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantas_gallardo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de plantas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'plantas_gallardo' debería activarse para 'Cuenta de plantas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de gallardo Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'plantas_gallardo' debería activarse para 'Cuenta de gallardo Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_plantas_gallardo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantas_gallardo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_augusto_maturana_match():
    """Debería activarse para cuentas con: augusto, maturana, plantacion"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'augusto_maturana'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de augusto Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'augusto_maturana' debería activarse para 'Cuenta de augusto Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de maturana Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'augusto_maturana' debería activarse para 'Cuenta de maturana Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_augusto_maturana_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'augusto_maturana'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_pacta_spa_match():
    """Debería activarse para cuentas con: pacta, spa, plantacion, tutores"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pacta_spa'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de pacta Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'pacta_spa' debería activarse para 'Cuenta de pacta Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de spa Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'pacta_spa' debería activarse para 'Cuenta de spa Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_pacta_spa_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pacta_spa'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_plantacion_plantas_match():
    """Debería activarse para cuentas con: plantacion, plantas, 4200593copeval"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_plantas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de plantacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'plantacion_plantas' debería activarse para 'Cuenta de plantacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de plantas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'plantacion_plantas' debería activarse para 'Cuenta de plantas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_plantacion_plantas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_plantas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_plantacion_tutores_match():
    """Debería activarse para cuentas con: plantacion, tutores, 4201196copeval"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_tutores'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de plantacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'plantacion_tutores' debería activarse para 'Cuenta de plantacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de tutores Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'plantacion_tutores' debería activarse para 'Cuenta de tutores Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_plantacion_tutores_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'plantacion_tutores'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_usufructo_roberto_match():
    """Debería activarse para cuentas con: usufructo, roberto, tra"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'usufructo_roberto'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de usufructo Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'usufructo_roberto' debería activarse para 'Cuenta de usufructo Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de roberto Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'usufructo_roberto' debería activarse para 'Cuenta de roberto Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_usufructo_roberto_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'usufructo_roberto'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_cuotas_anuales_match():
    """Debería activarse para cuentas con: cuotas, anuales"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cuotas_anuales'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de cuotas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'cuotas_anuales' debería activarse para 'Cuenta de cuotas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de anuales Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'cuotas_anuales' debería activarse para 'Cuenta de anuales Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_cuotas_anuales_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cuotas_anuales'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_amortizacion_2020_match():
    """Debería activarse para cuentas con: amortizacion, 2020"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'amortizacion_2020'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de amortizacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'amortizacion_2020' debería activarse para 'Cuenta de amortizacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 2020 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'amortizacion_2020' debería activarse para 'Cuenta de 2020 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_amortizacion_2020_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'amortizacion_2020'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_2020_sur_match():
    """Debería activarse para cuentas con: 2020, sur"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '2020_sur'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 2020 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '2020_sur' debería activarse para 'Cuenta de 2020 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de sur Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '2020_sur' debería activarse para 'Cuenta de sur Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_2020_sur_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '2020_sur'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_fernandez_boris_match():
    """Debería activarse para cuentas con: fernandez, boris"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'fernandez_boris'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de fernandez Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'fernandez_boris' debería activarse para 'Cuenta de fernandez Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de boris Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'fernandez_boris' debería activarse para 'Cuenta de boris Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_fernandez_boris_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'fernandez_boris'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_karina_garrido_match():
    """Debería activarse para cuentas con: karina, garrido"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'karina_garrido'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de karina Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'karina_garrido' debería activarse para 'Cuenta de karina Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de garrido Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'karina_garrido' debería activarse para 'Cuenta de garrido Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_karina_garrido_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'karina_garrido'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_jose_palavecino_match():
    """Debería activarse para cuentas con: jose, palavecino"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'jose_palavecino'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de jose Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'jose_palavecino' debería activarse para 'Cuenta de jose Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de palavecino Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'jose_palavecino' debería activarse para 'Cuenta de palavecino Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_jose_palavecino_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'jose_palavecino'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_carlos_chandia_match():
    """Debería activarse para cuentas con: carlos, chandia"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'carlos_chandia'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de carlos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'carlos_chandia' debería activarse para 'Cuenta de carlos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de chandia Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'carlos_chandia' debería activarse para 'Cuenta de chandia Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_carlos_chandia_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'carlos_chandia'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_resultados_asignado_match():
    """Debería activarse para cuentas con: resultados, asignado, ajuste, estado"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'resultados_asignado'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de resultados Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'resultados_asignado' debería activarse para 'Cuenta de resultados Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de asignado Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'resultados_asignado' debería activarse para 'Cuenta de asignado Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_resultados_asignado_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'resultados_asignado'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_reclasificacion_gastos_match():
    """Debería activarse para cuentas con: reclasificacion, gastos, 2009, 2010"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'reclasificacion_gastos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de reclasificacion Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'reclasificacion_gastos' debería activarse para 'Cuenta de reclasificacion Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de gastos Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'reclasificacion_gastos' debería activarse para 'Cuenta de gastos Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_reclasificacion_gastos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'reclasificacion_gastos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_dif_impto_match():
    """Debería activarse para cuentas con: dif, impto, 1era, cat, at2012"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'dif_impto'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de dif Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'dif_impto' debería activarse para 'Cuenta de dif Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de impto Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'dif_impto' debería activarse para 'Cuenta de impto Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_dif_impto_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'dif_impto'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_anticipos_ajuste_match():
    """Debería activarse para cuentas con: anticipos, ajuste, reclasificacion, proveedores"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'anticipos_ajuste'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de anticipos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'anticipos_ajuste' debería activarse para 'Cuenta de anticipos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de ajuste Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'anticipos_ajuste' debería activarse para 'Cuenta de ajuste Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_anticipos_ajuste_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'anticipos_ajuste'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_determinado_capital_match():
    """Debería activarse para cuentas con: determinado, capital, propio, inicial"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'determinado_capital'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de determinado Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'determinado_capital' debería activarse para 'Cuenta de determinado Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de capital Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'determinado_capital' debería activarse para 'Cuenta de capital Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_determinado_capital_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'determinado_capital'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_pretenciosa_12112_match():
    """Debería activarse para cuentas con: pretenciosa, 12112"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pretenciosa_12112'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de pretenciosa Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'pretenciosa_12112' debería activarse para 'Cuenta de pretenciosa Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 12112 Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'pretenciosa_12112' debería activarse para 'Cuenta de 12112 Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_pretenciosa_12112_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pretenciosa_12112'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_doberman_12116_match():
    """Debería activarse para cuentas con: doberman, 12116"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'doberman_12116'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de doberman Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'doberman_12116' debería activarse para 'Cuenta de doberman Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 12116 Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'doberman_12116' debería activarse para 'Cuenta de 12116 Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_doberman_12116_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'doberman_12116'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_12117_match():
    """Debería activarse para cuentas con: 12117"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '12117'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 12117 Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '12117' debería activarse para 'Cuenta de 12117 Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0


def test_12117_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '12117'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_queule_12122_match():
    """Debería activarse para cuentas con: queule, 12122"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'queule_12122'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de queule Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'queule_12122' debería activarse para 'Cuenta de queule Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 12122 Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'queule_12122' debería activarse para 'Cuenta de 12122 Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_queule_12122_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'queule_12122'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_12123_match():
    """Debería activarse para cuentas con: 12123"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '12123'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 12123 Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '12123' debería activarse para 'Cuenta de 12123 Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0


def test_12123_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '12123'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_luis_silva_match():
    """Debería activarse para cuentas con: luis, silva, plantacion, plantas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'luis_silva'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de luis Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'luis_silva' debería activarse para 'Cuenta de luis Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de silva Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'luis_silva' debería activarse para 'Cuenta de silva Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_luis_silva_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'luis_silva'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_135_comisiones_match():
    """Debería activarse para cuentas con: 135, comisiones, pagar, tarjeta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '135_comisiones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 135 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '135_comisiones' debería activarse para 'Cuenta de 135 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de comisiones Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '135_comisiones' debería activarse para 'Cuenta de comisiones Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_135_comisiones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '135_comisiones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_pasivos_oxros_match():
    """Debería activarse para cuentas con: pasivos, oxros, iealantes, otros"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pasivos_oxros'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de pasivos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'pasivos_oxros' debería activarse para 'Cuenta de pasivos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de oxros Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'pasivos_oxros' debería activarse para 'Cuenta de oxros Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_pasivos_oxros_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pasivos_oxros'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_inversiones_alternativas_match():
    """Debería activarse para cuentas con: inversiones, alternativas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'inversiones_alternativas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de inversiones Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'inversiones_alternativas' debería activarse para 'Cuenta de inversiones Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de alternativas Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'inversiones_alternativas' debería activarse para 'Cuenta de alternativas Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_inversiones_alternativas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'inversiones_alternativas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_part_cia_match():
    """Debería activarse para cuentas con: part, cia, cta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'part_cia'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de part Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'part_cia' debería activarse para 'Cuenta de part Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cia Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'part_cia' debería activarse para 'Cuenta de cia Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_part_cia_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'part_cia'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_bosques_csm_match():
    """Debería activarse para cuentas con: bosques, csm, ltda"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'bosques_csm'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de bosques Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'bosques_csm' debería activarse para 'Cuenta de bosques Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de csm Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'bosques_csm' debería activarse para 'Cuenta de csm Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_bosques_csm_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'bosques_csm'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_utilidad_perdida_match():
    """Debería activarse para cuentas con: utilidad, perdida, segun, balance"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'utilidad_perdida'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de utilidad Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'utilidad_perdida' debería activarse para 'Cuenta de utilidad Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de perdida Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'utilidad_perdida' debería activarse para 'Cuenta de perdida Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_utilidad_perdida_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'utilidad_perdida'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_goldman_sachs_match():
    """Debería activarse para cuentas con: goldman, sachs, comisiones, pagadas, gasto"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'goldman_sachs'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de goldman Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'goldman_sachs' debería activarse para 'Cuenta de goldman Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de sachs Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'goldman_sachs' debería activarse para 'Cuenta de sachs Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_goldman_sachs_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'goldman_sachs'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_seguros_generales_match():
    """Debería activarse para cuentas con: seguros, generales"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'seguros_generales'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de seguros Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'seguros_generales' debería activarse para 'Cuenta de seguros Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de generales Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'seguros_generales' debería activarse para 'Cuenta de generales Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_seguros_generales_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'seguros_generales'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_mercaderias_437_match():
    """Debería activarse para cuentas con: mercaderias, 437, 101, 434, capital"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mercaderias_437'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de mercaderias Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'mercaderias_437' debería activarse para 'Cuenta de mercaderias Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 437 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'mercaderias_437' debería activarse para 'Cuenta de 437 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_mercaderias_437_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mercaderias_437'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_subtotal_hectareas_match():
    """Debería activarse para cuentas con: subtotal, hectareas, productivas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'subtotal_hectareas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de subtotal Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'subtotal_hectareas' debería activarse para 'Cuenta de subtotal Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de hectareas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'subtotal_hectareas' debería activarse para 'Cuenta de hectareas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_subtotal_hectareas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'subtotal_hectareas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_construccion_parron_match():
    """Debería activarse para cuentas con: construccion, parron, nuevo, materiales"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'construccion_parron'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de construccion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'construccion_parron' debería activarse para 'Cuenta de construccion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de parron Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'construccion_parron' debería activarse para 'Cuenta de parron Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_construccion_parron_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'construccion_parron'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_licencias_programas_match():
    """Debería activarse para cuentas con: licencias, programas, comput, computacionales"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'licencias_programas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de licencias Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'licencias_programas' debería activarse para 'Cuenta de licencias Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de programas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'licencias_programas' debería activarse para 'Cuenta de programas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_licencias_programas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'licencias_programas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_operacional_resultado_match():
    """Debería activarse para cuentas con: operacional, resultado, totales, margen"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'operacional_resultado'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de operacional Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'operacional_resultado' debería activarse para 'Cuenta de operacional Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de resultado Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'operacional_resultado' debería activarse para 'Cuenta de resultado Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_operacional_resultado_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'operacional_resultado'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_patrimonio_neto_match():
    """Debería activarse para cuentas con: patrimonio, neto, atibas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'patrimonio_neto'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de patrimonio Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'patrimonio_neto' debería activarse para 'Cuenta de patrimonio Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de neto Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'patrimonio_neto' debería activarse para 'Cuenta de neto Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_patrimonio_neto_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'patrimonio_neto'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_remuneraciones_631_match():
    """Debería activarse para cuentas con: remuneraciones, 631, 1ssea36oplesa, eridas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'remuneraciones_631'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de remuneraciones Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'remuneraciones_631' debería activarse para 'Cuenta de remuneraciones Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 631 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'remuneraciones_631' debería activarse para 'Cuenta de 631 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_remuneraciones_631_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'remuneraciones_631'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_aldo_corriente_match():
    """Debería activarse para cuentas con: aldo, corriente, bancaria, rez"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'aldo_corriente'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de aldo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'aldo_corriente' debería activarse para 'Cuenta de aldo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de corriente Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'aldo_corriente' debería activarse para 'Cuenta de corriente Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_aldo_corriente_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'aldo_corriente'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_123_total_match():
    """Debería activarse para cuentas con: 123, total, pasivo, final, iaass7agtoli29"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '123_total'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 123 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '123_total' debería activarse para 'Cuenta de 123 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de total Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '123_total' debería activarse para 'Cuenta de total Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_123_total_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '123_total'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_resultado_negativo_match():
    """Debería activarse para cuentas con: resultado, negativo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'resultado_negativo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de resultado Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'resultado_negativo' debería activarse para 'Cuenta de resultado Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de negativo Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'resultado_negativo' debería activarse para 'Cuenta de negativo Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_resultado_negativo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'resultado_negativo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_otal_capital_match():
    """Debería activarse para cuentas con: otal, capital, enterado, rebaja, credito"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'otal_capital'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de otal Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'otal_capital' debería activarse para 'Cuenta de otal Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de capital Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'otal_capital' debería activarse para 'Cuenta de capital Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_otal_capital_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'otal_capital'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_gastos_977_match():
    """Debería activarse para cuentas con: gastos, 977, cobrar"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_977'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de gastos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'gastos_977' debería activarse para 'Cuenta de gastos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 977 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'gastos_977' debería activarse para 'Cuenta de 977 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_gastos_977_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_977'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_caja_392_match():
    """Debería activarse para cuentas con: caja, 392, cartas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'caja_392'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de caja Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'caja_392' debería activarse para 'Cuenta de caja Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 392 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'caja_392' debería activarse para 'Cuenta de 392 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_caja_392_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'caja_392'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_mercaderias_retencion_match():
    """Debería activarse para cuentas con: mercaderias, retencion, categoria, 10120633916"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mercaderias_retencion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de mercaderias Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'mercaderias_retencion' debería activarse para 'Cuenta de mercaderias Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de retencion Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'mercaderias_retencion' debería activarse para 'Cuenta de retencion Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_mercaderias_retencion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'mercaderias_retencion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_importacion_transito_match():
    """Debería activarse para cuentas con: importacion, transito, 245, 807"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'importacion_transito'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de importacion Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'importacion_transito' debería activarse para 'Cuenta de importacion Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de transito Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'importacion_transito' debería activarse para 'Cuenta de transito Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_importacion_transito_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'importacion_transito'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_edificios_capital_match():
    """Debería activarse para cuentas con: edificios, capital, propio, 1433522318, rey"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'edificios_capital'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de edificios Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'edificios_capital' debería activarse para 'Cuenta de edificios Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de capital Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'edificios_capital' debería activarse para 'Cuenta de capital Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_edificios_capital_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'edificios_capital'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_supermercados_rebajar_match():
    """Debería activarse para cuentas con: supermercados, rebajar"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'supermercados_rebajar'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de supermercados Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'supermercados_rebajar' debería activarse para 'Cuenta de supermercados Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de rebajar Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'supermercados_rebajar' debería activarse para 'Cuenta de rebajar Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_supermercados_rebajar_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'supermercados_rebajar'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_aldo_corriente_match():
    """Debería activarse para cuentas con: aldo, corriente"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'aldo_corriente'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de aldo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'aldo_corriente' debería activarse para 'Cuenta de aldo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de corriente Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'aldo_corriente' debería activarse para 'Cuenta de corriente Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_aldo_corriente_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'aldo_corriente'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_pagos_provisionales_match():
    """Debería activarse para cuentas con: pagos, provisionales, mensuales, 448, 100"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pagos_provisionales'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de pagos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'pagos_provisionales' debería activarse para 'Cuenta de pagos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de provisionales Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'pagos_provisionales' debería activarse para 'Cuenta de provisionales Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_pagos_provisionales_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pagos_provisionales'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_activo_fijo_match():
    """Debería activarse para cuentas con: activo, fijo, prestamo, banco, securidy"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'activo_fijo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de activo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'activo_fijo' debería activarse para 'Cuenta de activo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de fijo Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'activo_fijo' debería activarse para 'Cuenta de fijo Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_activo_fijo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'activo_fijo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_vehiculos_190_match():
    """Debería activarse para cuentas con: vehiculos, 190, pasivo, largo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'vehiculos_190'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de vehiculos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'vehiculos_190' debería activarse para 'Cuenta de vehiculos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 190 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'vehiculos_190' debería activarse para 'Cuenta de 190 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_vehiculos_190_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'vehiculos_190'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_servicios_bas_match():
    """Debería activarse para cuentas con: servicios, bas, pgresos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'servicios_bas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de servicios Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'servicios_bas' debería activarse para 'Cuenta de servicios Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de bas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'servicios_bas' debería activarse para 'Cuenta de bas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_servicios_bas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'servicios_bas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_edificios_inmuebles_match():
    """Debería activarse para cuentas con: edificios, inmuebles"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'edificios_inmuebles'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de edificios Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'edificios_inmuebles' debería activarse para 'Cuenta de edificios Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de inmuebles Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'edificios_inmuebles' debería activarse para 'Cuenta de inmuebles Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_edificios_inmuebles_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'edificios_inmuebles'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_polo_spa_match():
    """Debería activarse para cuentas con: polo, spa, 13104"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'polo_spa'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de polo Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'polo_spa' debería activarse para 'Cuenta de polo Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de spa Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'polo_spa' debería activarse para 'Cuenta de spa Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_polo_spa_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'polo_spa'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_resultados_2017_match():
    """Debería activarse para cuentas con: resultados, 2017, total, estados"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'resultados_2017'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de resultados Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'resultados_2017' debería activarse para 'Cuenta de resultados Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 2017 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'resultados_2017' debería activarse para 'Cuenta de 2017 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_resultados_2017_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'resultados_2017'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_santa_maria_match():
    """Debería activarse para cuentas con: santa, maria, direccion, rendicion, super"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'santa_maria'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de santa Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'santa_maria' debería activarse para 'Cuenta de santa Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de maria Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'santa_maria' debería activarse para 'Cuenta de maria Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_santa_maria_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'santa_maria'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_bancos_prestamos_match():
    """Debería activarse para cuentas con: bancos, prestamos, 934"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'bancos_prestamos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de bancos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'bancos_prestamos' debería activarse para 'Cuenta de bancos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de prestamos Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'bancos_prestamos' debería activarse para 'Cuenta de prestamos Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_bancos_prestamos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'bancos_prestamos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_dolares_menor_match():
    """Debería activarse para cuentas con: dolares, menor, mayor, valor"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'dolares_menor'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de dolares Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'dolares_menor' debería activarse para 'Cuenta de dolares Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de menor Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'dolares_menor' debería activarse para 'Cuenta de menor Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_dolares_menor_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'dolares_menor'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_fdo_util_match():
    """Debería activarse para cuentas con: fdo, util, trib, acumuladas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'fdo_util'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de fdo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'fdo_util' debería activarse para 'Cuenta de fdo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de util Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'fdo_util' debería activarse para 'Cuenta de util Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_fdo_util_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'fdo_util'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_prestano_bci_match():
    """Debería activarse para cuentas con: prestano, bci, 123696, c66pabna"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'prestano_bci'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de prestano Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'prestano_bci' debería activarse para 'Cuenta de prestano Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de bci Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'prestano_bci' debería activarse para 'Cuenta de bci Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_prestano_bci_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'prestano_bci'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_gastos_generales_match():
    """Debería activarse para cuentas con: gastos, generales, 19315181"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_generales'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de gastos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'gastos_generales' debería activarse para 'Cuenta de gastos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de generales Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'gastos_generales' debería activarse para 'Cuenta de generales Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_gastos_generales_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_generales'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_subtotale_2575499743_match():
    """Debería activarse para cuentas con: subtotale, 2575499743, 2390901598, 1829628222"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'subtotale_2575499743'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de subtotale Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'subtotale_2575499743' debería activarse para 'Cuenta de subtotale Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 2575499743 Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'subtotale_2575499743' debería activarse para 'Cuenta de 2575499743 Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_subtotale_2575499743_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'subtotale_2575499743'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_inpto_renta_match():
    """Debería activarse para cuentas con: inpto, renta, 4334333, 10cat6"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'inpto_renta'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de inpto Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'inpto_renta' debería activarse para 'Cuenta de inpto Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de renta Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'inpto_renta' debería activarse para 'Cuenta de renta Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_inpto_renta_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'inpto_renta'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_4101_fletes_match():
    """Debería activarse para cuentas con: 4101, fletes"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '4101_fletes'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 4101 Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '4101_fletes' debería activarse para 'Cuenta de 4101 Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de fletes Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '4101_fletes' debería activarse para 'Cuenta de fletes Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_4101_fletes_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '4101_fletes'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_seguro_accidentes_match():
    """Debería activarse para cuentas con: seguro, accidentes, 4202, 4102"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'seguro_accidentes'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de seguro Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'seguro_accidentes' debería activarse para 'Cuenta de seguro Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de accidentes Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'seguro_accidentes' debería activarse para 'Cuenta de accidentes Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_seguro_accidentes_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'seguro_accidentes'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_4103_seguros_match():
    """Debería activarse para cuentas con: 4103, seguros"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '4103_seguros'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 4103 Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '4103_seguros' debería activarse para 'Cuenta de 4103 Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de seguros Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '4103_seguros' debería activarse para 'Cuenta de seguros Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_4103_seguros_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '4103_seguros'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_4103_gastos_match():
    """Debería activarse para cuentas con: 4103, gastos, 341"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '4103_gastos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 4103 Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '4103_gastos' debería activarse para 'Cuenta de 4103 Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de gastos Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '4103_gastos' debería activarse para 'Cuenta de gastos Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_4103_gastos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '4103_gastos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_2109_match():
    """Debería activarse para cuentas con: 2109"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '2109'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 2109 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '2109' debería activarse para 'Cuenta de 2109 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0


def test_2109_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '2109'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_servicios_asesorias_match():
    """Debería activarse para cuentas con: servicios, asesorias, basicos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'servicios_asesorias'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de servicios Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'servicios_asesorias' debería activarse para 'Cuenta de servicios Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de asesorias Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'servicios_asesorias' debería activarse para 'Cuenta de asesorias Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_servicios_asesorias_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'servicios_asesorias'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_embotellacion_gasto_match():
    """Debería activarse para cuentas con: embotellacion, gasto, gastos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'embotellacion_gasto'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de embotellacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'embotellacion_gasto' debería activarse para 'Cuenta de embotellacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de gasto Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'embotellacion_gasto' debería activarse para 'Cuenta de gasto Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_embotellacion_gasto_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'embotellacion_gasto'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_gasto_mant_match():
    """Debería activarse para cuentas con: gasto, mant, destilatorio, 1117312"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gasto_mant'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de gasto Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'gasto_mant' debería activarse para 'Cuenta de gasto Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de mant Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'gasto_mant' debería activarse para 'Cuenta de mant Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_gasto_mant_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gasto_mant'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_materia_prima_match():
    """Debería activarse para cuentas con: materia, prima, 66137878"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'materia_prima'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de materia Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'materia_prima' debería activarse para 'Cuenta de materia Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de prima Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'materia_prima' debería activarse para 'Cuenta de prima Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_materia_prima_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'materia_prima'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_remuneracion_personal_match():
    """Debería activarse para cuentas con: remuneracion, personal, perman, permanente101, 002"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'remuneracion_personal'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de remuneracion Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'remuneracion_personal' debería activarse para 'Cuenta de remuneracion Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de personal Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'remuneracion_personal' debería activarse para 'Cuenta de personal Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_remuneracion_personal_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'remuneracion_personal'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_remunerac_temporeros_match():
    """Debería activarse para cuentas con: remunerac, temporeros"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'remunerac_temporeros'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de remunerac Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'remunerac_temporeros' debería activarse para 'Cuenta de remunerac Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de temporeros Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'remunerac_temporeros' debería activarse para 'Cuenta de temporeros Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_remunerac_temporeros_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'remunerac_temporeros'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_foliares_match():
    """Debería activarse para cuentas con: foliares"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'foliares'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de foliares Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'foliares' debería activarse para 'Cuenta de foliares Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0


def test_foliares_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'foliares'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_costos_contratista_match():
    """Debería activarse para cuentas con: costos, contratista"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'costos_contratista'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de costos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'costos_contratista' debería activarse para 'Cuenta de costos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de contratista Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'costos_contratista' debería activarse para 'Cuenta de contratista Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_costos_contratista_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'costos_contratista'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_reparaciones_plantaciones_match():
    """Debería activarse para cuentas con: reparaciones, plantaciones"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'reparaciones_plantaciones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de reparaciones Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'reparaciones_plantaciones' debería activarse para 'Cuenta de reparaciones Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de plantaciones Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'reparaciones_plantaciones' debería activarse para 'Cuenta de plantaciones Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_reparaciones_plantaciones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'reparaciones_plantaciones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_rep_const_match():
    """Debería activarse para cuentas con: rep, const, habitacionales"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'rep_const'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de rep Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'rep_const' debería activarse para 'Cuenta de rep Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de const Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'rep_const' debería activarse para 'Cuenta de const Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_rep_const_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'rep_const'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_transporte_carga_match():
    """Debería activarse para cuentas con: transporte, carga, corresp"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'transporte_carga'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de transporte Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'transporte_carga' debería activarse para 'Cuenta de transporte Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de carga Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'transporte_carga' debería activarse para 'Cuenta de carga Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_transporte_carga_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'transporte_carga'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_arriendo_bienes_match():
    """Debería activarse para cuentas con: arriendo, bienes, ministeric"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'arriendo_bienes'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de arriendo Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'arriendo_bienes' debería activarse para 'Cuenta de arriendo Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de bienes Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'arriendo_bienes' debería activarse para 'Cuenta de bienes Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_arriendo_bienes_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'arriendo_bienes'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_sueldos_empresarial_match():
    """Debería activarse para cuentas con: sueldos, empresarial"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'sueldos_empresarial'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de sueldos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'sueldos_empresarial' debería activarse para 'Cuenta de sueldos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de empresarial Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'sueldos_empresarial' debería activarse para 'Cuenta de empresarial Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_sueldos_empresarial_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'sueldos_empresarial'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_ajustes_varios_match():
    """Debería activarse para cuentas con: ajustes, varios"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ajustes_varios'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de ajustes Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'ajustes_varios' debería activarse para 'Cuenta de ajustes Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de varios Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'ajustes_varios' debería activarse para 'Cuenta de varios Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_ajustes_varios_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'ajustes_varios'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_impto_territorial_match():
    """Debería activarse para cuentas con: impto, territorial"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'impto_territorial'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de impto Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'impto_territorial' debería activarse para 'Cuenta de impto Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de territorial Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'impto_territorial' debería activarse para 'Cuenta de territorial Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_impto_territorial_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'impto_territorial'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_corr_mon_match():
    """Debería activarse para cuentas con: corr, mon, patrimonio"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'corr_mon'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de corr Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'corr_mon' debería activarse para 'Cuenta de corr Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de mon Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'corr_mon' debería activarse para 'Cuenta de mon Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_corr_mon_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'corr_mon'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_reajustes_favorables_match():
    """Debería activarse para cuentas con: reajustes, favorables, desfavorables"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'reajustes_favorables'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de reajustes Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'reajustes_favorables' debería activarse para 'Cuenta de reajustes Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de favorables Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'reajustes_favorables' debería activarse para 'Cuenta de favorables Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_reajustes_favorables_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'reajustes_favorables'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_otros_ing_match():
    """Debería activarse para cuentas con: otros, ing, mercad, bonificad"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'otros_ing'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de otros Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'otros_ing' debería activarse para 'Cuenta de otros Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de ing Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'otros_ing' debería activarse para 'Cuenta de ing Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_otros_ing_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'otros_ing'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_costo_polla_match():
    """Debería activarse para cuentas con: costo, polla, chilena"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'costo_polla'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de costo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'costo_polla' debería activarse para 'Cuenta de costo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de polla Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'costo_polla' debería activarse para 'Cuenta de polla Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_costo_polla_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'costo_polla'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_costo_recarga_match():
    """Debería activarse para cuentas con: costo, recarga, telefonica"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'costo_recarga'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de costo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'costo_recarga' debería activarse para 'Cuenta de costo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de recarga Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'costo_recarga' debería activarse para 'Cuenta de recarga Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_costo_recarga_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'costo_recarga'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_comision_comercio_match():
    """Debería activarse para cuentas con: comision, comercio, asociado"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'comision_comercio'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de comision Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'comision_comercio' debería activarse para 'Cuenta de comision Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de comercio Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'comision_comercio' debería activarse para 'Cuenta de comercio Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_comision_comercio_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'comision_comercio'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_gastos_repr_match():
    """Debería activarse para cuentas con: gastos, repr, gerencia, pasajes"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_repr'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de gastos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'gastos_repr' debería activarse para 'Cuenta de gastos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de repr Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'gastos_repr' debería activarse para 'Cuenta de repr Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_gastos_repr_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_repr'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_gastos_hospedaje_match():
    """Debería activarse para cuentas con: gastos, hospedaje, colaciones"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_hospedaje'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de gastos Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'gastos_hospedaje' debería activarse para 'Cuenta de gastos Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de hospedaje Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'gastos_hospedaje' debería activarse para 'Cuenta de hospedaje Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_gastos_hospedaje_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gastos_hospedaje'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_servicios_traslado_match():
    """Debería activarse para cuentas con: servicios, traslado, valores"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'servicios_traslado'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de servicios Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'servicios_traslado' debería activarse para 'Cuenta de servicios Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de traslado Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'servicios_traslado' debería activarse para 'Cuenta de traslado Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_servicios_traslado_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'servicios_traslado'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_recoleccion_residuos_match():
    """Debería activarse para cuentas con: recoleccion, residuos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'recoleccion_residuos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de recoleccion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'recoleccion_residuos' debería activarse para 'Cuenta de recoleccion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de residuos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'recoleccion_residuos' debería activarse para 'Cuenta de residuos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_recoleccion_residuos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'recoleccion_residuos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_santander_obligaciones_match():
    """Debería activarse para cuentas con: santander, obligaciones, banco, comision"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'santander_obligaciones'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de santander Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'santander_obligaciones' debería activarse para 'Cuenta de santander Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de obligaciones Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'santander_obligaciones' debería activarse para 'Cuenta de obligaciones Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_santander_obligaciones_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'santander_obligaciones'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_impuesto_primera_match():
    """Debería activarse para cuentas con: impuesto, primera, provisional, categora, aero"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'impuesto_primera'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de impuesto Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'impuesto_primera' debería activarse para 'Cuenta de impuesto Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de primera Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'impuesto_primera' debería activarse para 'Cuenta de primera Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_impuesto_primera_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'impuesto_primera'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_bbva_scotiabank_match():
    """Debería activarse para cuentas con: bbva, scotiabank, banco, linea"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'bbva_scotiabank'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de bbva Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'bbva_scotiabank' debería activarse para 'Cuenta de bbva Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de scotiabank Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'bbva_scotiabank' debería activarse para 'Cuenta de scotiabank Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_bbva_scotiabank_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'bbva_scotiabank'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_raps_match():
    """Debería activarse para cuentas con: raps"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'raps'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de raps Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'raps' debería activarse para 'Cuenta de raps Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0


def test_raps_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'raps'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_maiz_semilla_match():
    """Debería activarse para cuentas con: maiz, semilla"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'maiz_semilla'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de maiz Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'maiz_semilla' debería activarse para 'Cuenta de maiz Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de semilla Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'maiz_semilla' debería activarse para 'Cuenta de semilla Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_maiz_semilla_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'maiz_semilla'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_esparragos_esparraguera_match():
    """Debería activarse para cuentas con: esparragos, esparraguera"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'esparragos_esparraguera'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de esparragos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'esparragos_esparraguera' debería activarse para 'Cuenta de esparragos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de esparraguera Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'esparragos_esparraguera' debería activarse para 'Cuenta de esparraguera Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_esparragos_esparraguera_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'esparragos_esparraguera'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_iva_retenido_match():
    """Debería activarse para cuentas con: iva, retenido, parcial"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'iva_retenido'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de iva Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'iva_retenido' debería activarse para 'Cuenta de iva Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de retenido Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'iva_retenido' debería activarse para 'Cuenta de retenido Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_iva_retenido_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'iva_retenido'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_retenciones_2da_match():
    """Debería activarse para cuentas con: retenciones, 2da, categoria"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'retenciones_2da'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de retenciones Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'retenciones_2da' debería activarse para 'Cuenta de retenciones Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de 2da Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'retenciones_2da' debería activarse para 'Cuenta de 2da Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_retenciones_2da_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'retenciones_2da'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_cta_particular_match():
    """Debería activarse para cuentas con: cta, particular, edgardo, meynet"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cta_particular'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de cta Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'cta_particular' debería activarse para 'Cuenta de cta Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de particular Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'cta_particular' debería activarse para 'Cuenta de particular Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_cta_particular_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cta_particular'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_arriendo_quimicos_match():
    """Debería activarse para cuentas con: arriendo, quimicos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'arriendo_quimicos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de arriendo Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'arriendo_quimicos' debería activarse para 'Cuenta de arriendo Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de quimicos Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'arriendo_quimicos' debería activarse para 'Cuenta de quimicos Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_arriendo_quimicos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'arriendo_quimicos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_antimalezas_match():
    """Debería activarse para cuentas con: antimalezas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'antimalezas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de antimalezas Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'antimalezas' debería activarse para 'Cuenta de antimalezas Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0


def test_antimalezas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'antimalezas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_pesa_aaa_match():
    """Debería activarse para cuentas con: pesa, aaa"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pesa_aaa'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de pesa Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'pesa_aaa' debería activarse para 'Cuenta de pesa Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de aaa Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'pesa_aaa' debería activarse para 'Cuenta de aaa Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_pesa_aaa_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'pesa_aaa'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_operacion_pendiente_match():
    """Debería activarse para cuentas con: operacion, pendiente"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'operacion_pendiente'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de operacion Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'operacion_pendiente' debería activarse para 'Cuenta de operacion Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de pendiente Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'operacion_pendiente' debería activarse para 'Cuenta de pendiente Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_operacion_pendiente_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'operacion_pendiente'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_cta_cte_match():
    """Debería activarse para cuentas con: cta, cte, trabajadores, auxiliar"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cta_cte'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de cta Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'cta_cte' debería activarse para 'Cuenta de cta Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cte Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'cta_cte' debería activarse para 'Cuenta de cte Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_cta_cte_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'cta_cte'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_bonificaciones_recuperar_match():
    """Debería activarse para cuentas con: bonificaciones, recuperar"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'bonificaciones_recuperar'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de bonificaciones Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'bonificaciones_recuperar' debería activarse para 'Cuenta de bonificaciones Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de recuperar Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'bonificaciones_recuperar' debería activarse para 'Cuenta de recuperar Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_bonificaciones_recuperar_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'bonificaciones_recuperar'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_facturas_reclasificar_match():
    """Debería activarse para cuentas con: facturas, reclasificar"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'facturas_reclasificar'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de facturas Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'facturas_reclasificar' debería activarse para 'Cuenta de facturas Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de reclasificar Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'facturas_reclasificar' debería activarse para 'Cuenta de reclasificar Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_facturas_reclasificar_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'facturas_reclasificar'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_general_borgo_match():
    """Debería activarse para cuentas con: general, borgo, 934"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'general_borgo'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de general Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'general_borgo' debería activarse para 'Cuenta de general Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de borgo Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'general_borgo' debería activarse para 'Cuenta de borgo Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_general_borgo_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'general_borgo'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_asociacion_chilena_match():
    """Debería activarse para cuentas con: asociacion, chilena, 2222, seg, azos6si"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'asociacion_chilena'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de asociacion Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'asociacion_chilena' debería activarse para 'Cuenta de asociacion Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de chilena Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'asociacion_chilena' debería activarse para 'Cuenta de chilena Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_asociacion_chilena_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'asociacion_chilena'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_30110venta_granel_match():
    """Debería activarse para cuentas con: 30110venta, granel"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '30110venta_granel'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 30110venta Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '30110venta_granel' debería activarse para 'Cuenta de 30110venta Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de granel Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '30110venta_granel' debería activarse para 'Cuenta de granel Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_30110venta_granel_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '30110venta_granel'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_30150intereses_bancos_match():
    """Debería activarse para cuentas con: 30150intereses, bancos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '30150intereses_bancos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 30150intereses Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '30150intereses_bancos' debería activarse para 'Cuenta de 30150intereses Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de bancos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '30150intereses_bancos' debería activarse para 'Cuenta de bancos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_30150intereses_bancos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '30150intereses_bancos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_30290venta_material_match():
    """Debería activarse para cuentas con: 30290venta, material, desecho"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '30290venta_material'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 30290venta Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '30290venta_material' debería activarse para 'Cuenta de 30290venta Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de material Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '30290venta_material' debería activarse para 'Cuenta de material Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_30290venta_material_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '30290venta_material'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_30360servicio_vinificacion_match():
    """Debería activarse para cuentas con: 30360servicio, vinificacion"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '30360servicio_vinificacion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 30360servicio Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '30360servicio_vinificacion' debería activarse para 'Cuenta de 30360servicio Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de vinificacion Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '30360servicio_vinificacion' debería activarse para 'Cuenta de vinificacion Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_30360servicio_vinificacion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '30360servicio_vinificacion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_30370ventas_estacas_match():
    """Debería activarse para cuentas con: 30370ventas, estacas, pasto, otros"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '30370ventas_estacas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 30370ventas Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '30370ventas_estacas' debería activarse para 'Cuenta de 30370ventas Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de estacas Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '30370ventas_estacas' debería activarse para 'Cuenta de estacas Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_30370ventas_estacas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '30370ventas_estacas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_30491diferencia_precios_match():
    """Debería activarse para cuentas con: 30491diferencia, precios, vta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '30491diferencia_precios'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 30491diferencia Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '30491diferencia_precios' debería activarse para 'Cuenta de 30491diferencia Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de precios Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '30491diferencia_precios' debería activarse para 'Cuenta de precios Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_30491diferencia_precios_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '30491diferencia_precios'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_40141merma_proceso_match():
    """Debería activarse para cuentas con: 40141merma, proceso, productivo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40141merma_proceso'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 40141merma Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '40141merma_proceso' debería activarse para 'Cuenta de 40141merma Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de proceso Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '40141merma_proceso' debería activarse para 'Cuenta de proceso Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_40141merma_proceso_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40141merma_proceso'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_40160gastos_variables_match():
    """Debería activarse para cuentas con: 40160gastos, variables, venta"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40160gastos_variables'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 40160gastos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '40160gastos_variables' debería activarse para 'Cuenta de 40160gastos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de variables Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '40160gastos_variables' debería activarse para 'Cuenta de variables Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_40160gastos_variables_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40160gastos_variables'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_40165gastos_variables_match():
    """Debería activarse para cuentas con: 40165gastos, variables, reproceso, vino"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40165gastos_variables'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 40165gastos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '40165gastos_variables' debería activarse para 'Cuenta de 40165gastos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de variables Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '40165gastos_variables' debería activarse para 'Cuenta de variables Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_40165gastos_variables_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40165gastos_variables'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_40166comision_vta_match():
    """Debería activarse para cuentas con: 40166comision, vta, uva, vino"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40166comision_vta'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 40166comision Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '40166comision_vta' debería activarse para 'Cuenta de 40166comision Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de vta Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '40166comision_vta' debería activarse para 'Cuenta de vta Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_40166comision_vta_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40166comision_vta'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_40310diferencias_inventario_match():
    """Debería activarse para cuentas con: 40310diferencias, inventario"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40310diferencias_inventario'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 40310diferencias Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '40310diferencias_inventario' debería activarse para 'Cuenta de 40310diferencias Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de inventario Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '40310diferencias_inventario' debería activarse para 'Cuenta de inventario Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_40310diferencias_inventario_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40310diferencias_inventario'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_40311diferencia_cubicacion_match():
    """Debería activarse para cuentas con: 40311diferencia, cubicacion, huinchas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40311diferencia_cubicacion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 40311diferencia Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '40311diferencia_cubicacion' debería activarse para 'Cuenta de 40311diferencia Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de cubicacion Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '40311diferencia_cubicacion' debería activarse para 'Cuenta de cubicacion Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_40311diferencia_cubicacion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40311diferencia_cubicacion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_40360costo_vta_match():
    """Debería activarse para cuentas con: 40360costo, vta, insumo, mat"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40360costo_vta'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 40360costo Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '40360costo_vta' debería activarse para 'Cuenta de 40360costo Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de vta Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '40360costo_vta' debería activarse para 'Cuenta de vta Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_40360costo_vta_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40360costo_vta'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_40410cto_vta_match():
    """Debería activarse para cuentas con: 40410cto, vta, agroquim"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40410cto_vta'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 40410cto Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '40410cto_vta' debería activarse para 'Cuenta de 40410cto Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de vta Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '40410cto_vta' debería activarse para 'Cuenta de vta Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_40410cto_vta_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40410cto_vta'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_40430otros_egresos_match():
    """Debería activarse para cuentas con: 40430otros, egresos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40430otros_egresos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 40430otros Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '40430otros_egresos' debería activarse para 'Cuenta de 40430otros Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de egresos Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '40430otros_egresos' debería activarse para 'Cuenta de egresos Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_40430otros_egresos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40430otros_egresos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_40431multas_match():
    """Debería activarse para cuentas con: 40431multas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40431multas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 40431multas Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '40431multas' debería activarse para 'Cuenta de 40431multas Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0


def test_40431multas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40431multas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_40460deudores_incobrables_match():
    """Debería activarse para cuentas con: 40460deudores, incobrables"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40460deudores_incobrables'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 40460deudores Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '40460deudores_incobrables' debería activarse para 'Cuenta de 40460deudores Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de incobrables Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '40460deudores_incobrables' debería activarse para 'Cuenta de incobrables Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_40460deudores_incobrables_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40460deudores_incobrables'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_40500impuesto_diferido_match():
    """Debería activarse para cuentas con: 40500impuesto, diferido"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40500impuesto_diferido'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 40500impuesto Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '40500impuesto_diferido' debería activarse para 'Cuenta de 40500impuesto Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de diferido Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '40500impuesto_diferido' debería activarse para 'Cuenta de diferido Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_40500impuesto_diferido_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '40500impuesto_diferido'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50040remun_base_match():
    """Debería activarse para cuentas con: 50040remun, base, personal"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50040remun_base'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50040remun Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50040remun_base' debería activarse para 'Cuenta de 50040remun Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de base Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '50040remun_base' debería activarse para 'Cuenta de base Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_50040remun_base_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50040remun_base'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50050sobretiempo_personal_match():
    """Debería activarse para cuentas con: 50050sobretiempo, personal"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50050sobretiempo_personal'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50050sobretiempo Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50050sobretiempo_personal' debería activarse para 'Cuenta de 50050sobretiempo Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de personal Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '50050sobretiempo_personal' debería activarse para 'Cuenta de personal Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_50050sobretiempo_personal_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50050sobretiempo_personal'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50060gratificacion_personal_match():
    """Debería activarse para cuentas con: 50060gratificacion, personal"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50060gratificacion_personal'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50060gratificacion Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50060gratificacion_personal' debería activarse para 'Cuenta de 50060gratificacion Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de personal Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '50060gratificacion_personal' debería activarse para 'Cuenta de personal Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_50060gratificacion_personal_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50060gratificacion_personal'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50120seguro_acciden_match():
    """Debería activarse para cuentas con: 50120seguro, acciden, trabajo"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50120seguro_acciden'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50120seguro Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50120seguro_acciden' debería activarse para 'Cuenta de 50120seguro Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de acciden Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '50120seguro_acciden' debería activarse para 'Cuenta de acciden Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_50120seguro_acciden_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50120seguro_acciden'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50130feriado_benef_match():
    """Debería activarse para cuentas con: 50130feriado, benef, garantizado"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50130feriado_benef'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50130feriado Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50130feriado_benef' debería activarse para 'Cuenta de 50130feriado Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de benef Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '50130feriado_benef' debería activarse para 'Cuenta de benef Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_50130feriado_benef_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50130feriado_benef'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50150seguro_salud_match():
    """Debería activarse para cuentas con: 50150seguro, salud, complementario"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50150seguro_salud'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50150seguro Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50150seguro_salud' debería activarse para 'Cuenta de 50150seguro Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de salud Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '50150seguro_salud' debería activarse para 'Cuenta de salud Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_50150seguro_salud_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50150seguro_salud'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50160alimentac_alojam_match():
    """Debería activarse para cuentas con: 50160alimentac, alojam, trabajador"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50160alimentac_alojam'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50160alimentac Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50160alimentac_alojam' debería activarse para 'Cuenta de 50160alimentac Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de alojam Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '50160alimentac_alojam' debería activarse para 'Cuenta de alojam Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_50160alimentac_alojam_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50160alimentac_alojam'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50170movilizac_trabajadores_match():
    """Debería activarse para cuentas con: 50170movilizac, trabajadores"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50170movilizac_trabajadores'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50170movilizac Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50170movilizac_trabajadores' debería activarse para 'Cuenta de 50170movilizac Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de trabajadores Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '50170movilizac_trabajadores' debería activarse para 'Cuenta de trabajadores Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_50170movilizac_trabajadores_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50170movilizac_trabajadores'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50190elementos_seguridad_match():
    """Debería activarse para cuentas con: 50190elementos, seguridad, prote"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50190elementos_seguridad'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50190elementos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50190elementos_seguridad' debería activarse para 'Cuenta de 50190elementos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de seguridad Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '50190elementos_seguridad' debería activarse para 'Cuenta de seguridad Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_50190elementos_seguridad_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50190elementos_seguridad'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50200otros_benef_match():
    """Debería activarse para cuentas con: 50200otros, benef, trabajadores"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50200otros_benef'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50200otros Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50200otros_benef' debería activarse para 'Cuenta de 50200otros Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de benef Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '50200otros_benef' debería activarse para 'Cuenta de benef Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_50200otros_benef_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50200otros_benef'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50351honorarios_serv_match():
    """Debería activarse para cuentas con: 50351honorarios, serv, adm, agricola"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50351honorarios_serv'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50351honorarios Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50351honorarios_serv' debería activarse para 'Cuenta de 50351honorarios Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de serv Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '50351honorarios_serv' debería activarse para 'Cuenta de serv Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_50351honorarios_serv_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50351honorarios_serv'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50390servicios_match():
    """Debería activarse para cuentas con: 50390servicios"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50390servicios'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50390servicios Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50390servicios' debería activarse para 'Cuenta de 50390servicios Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0


def test_50390servicios_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50390servicios'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50410desarrollo_promocion_match():
    """Debería activarse para cuentas con: 50410desarrollo, promocion"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50410desarrollo_promocion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50410desarrollo Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50410desarrollo_promocion' debería activarse para 'Cuenta de 50410desarrollo Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de promocion Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '50410desarrollo_promocion' debería activarse para 'Cuenta de promocion Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_50410desarrollo_promocion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50410desarrollo_promocion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50490seguros_match():
    """Debería activarse para cuentas con: 50490seguros"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50490seguros'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50490seguros Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50490seguros' debería activarse para 'Cuenta de 50490seguros Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0


def test_50490seguros_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50490seguros'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50510arriendos_match():
    """Debería activarse para cuentas con: 50510arriendos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50510arriendos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50510arriendos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50510arriendos' debería activarse para 'Cuenta de 50510arriendos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0


def test_50510arriendos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50510arriendos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50590suministros_match():
    """Debería activarse para cuentas con: 50590suministros"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50590suministros'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50590suministros Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50590suministros' debería activarse para 'Cuenta de 50590suministros Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0


def test_50590suministros_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50590suministros'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50630insumos_agricolas_match():
    """Debería activarse para cuentas con: 50630insumos, agricolas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50630insumos_agricolas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50630insumos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50630insumos_agricolas' debería activarse para 'Cuenta de 50630insumos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de agricolas Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '50630insumos_agricolas' debería activarse para 'Cuenta de agricolas Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_50630insumos_agricolas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50630insumos_agricolas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50670amortizacion_intangibles_match():
    """Debería activarse para cuentas con: 50670amortizacion, intangibles"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50670amortizacion_intangibles'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50670amortizacion Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50670amortizacion_intangibles' debería activarse para 'Cuenta de 50670amortizacion Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de intangibles Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '50670amortizacion_intangibles' debería activarse para 'Cuenta de intangibles Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_50670amortizacion_intangibles_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50670amortizacion_intangibles'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50691gastos_aceptados_match():
    """Debería activarse para cuentas con: 50691gastos, aceptados"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50691gastos_aceptados'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50691gastos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50691gastos_aceptados' debería activarse para 'Cuenta de 50691gastos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de aceptados Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '50691gastos_aceptados' debería activarse para 'Cuenta de aceptados Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_50691gastos_aceptados_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50691gastos_aceptados'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_50710provision_gastos_match():
    """Debería activarse para cuentas con: 50710provision, gastos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50710provision_gastos'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 50710provision Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '50710provision_gastos' debería activarse para 'Cuenta de 50710provision Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de gastos Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '50710provision_gastos' debería activarse para 'Cuenta de gastos Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_50710provision_gastos_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '50710provision_gastos'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_balance_general_match():
    """Debería activarse para cuentas con: balance, general"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'balance_general'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de balance Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'balance_general' debería activarse para 'Cuenta de balance Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de general Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'balance_general' debería activarse para 'Cuenta de general Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_balance_general_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'balance_general'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_aporte_match():
    """Debería activarse para cuentas con: aporte"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'aporte'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de aporte Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'aporte' debería activarse para 'Cuenta de aporte Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0


def test_aporte_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'aporte'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_0223_servicios_match():
    """Debería activarse para cuentas con: 0223, servicios, basicos"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '0223_servicios'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de 0223 Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla '0223_servicios' debería activarse para 'Cuenta de 0223 Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de servicios Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla '0223_servicios' debería activarse para 'Cuenta de servicios Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_0223_servicios_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == '0223_servicios'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_oooooooooooooooooooooo_o1n_match():
    """Debería activarse para cuentas con: oooooooooooooooooooooo, o1n"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'oooooooooooooooooooooo_o1n'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de oooooooooooooooooooooo Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'oooooooooooooooooooooo_o1n' debería activarse para 'Cuenta de oooooooooooooooooooooo Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de o1n Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'oooooooooooooooooooooo_o1n' debería activarse para 'Cuenta de o1n Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_oooooooooooooooooooooo_o1n_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'oooooooooooooooooooooo_o1n'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_servicios_contabilidad_match():
    """Debería activarse para cuentas con: servicios, contabilidad, contables"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'servicios_contabilidad'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de servicios Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'servicios_contabilidad' debería activarse para 'Cuenta de servicios Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de contabilidad Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'servicios_contabilidad' debería activarse para 'Cuenta de contabilidad Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_servicios_contabilidad_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'servicios_contabilidad'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_gasto_instalacion_match():
    """Debería activarse para cuentas con: gasto, instalacion"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gasto_instalacion'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de gasto Test 0",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'gasto_instalacion' debería activarse para 'Cuenta de gasto Test 0''
    assert result_0.semantic_type == "revenue"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de instalacion Test 1",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'gasto_instalacion' debería activarse para 'Cuenta de instalacion Test 1''
    assert result_1.semantic_type == "revenue"
    assert result_1.confidence > 0.0


def test_gasto_instalacion_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'gasto_instalacion'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="profit",
        amounts=MonetaryAmounts(profits=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_proyecto_luminaria_match():
    """Debería activarse para cuentas con: proyecto, luminaria, rainier"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'proyecto_luminaria'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de proyecto Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'proyecto_luminaria' debería activarse para 'Cuenta de proyecto Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de luminaria Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'proyecto_luminaria' debería activarse para 'Cuenta de luminaria Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_proyecto_luminaria_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'proyecto_luminaria'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None


def test_productos_agricolas_match():
    """Debería activarse para cuentas con: productos, agricolas, 404, 864"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'productos_agricolas'), None)
    assert rule is not None, f'Regla {rule_name!r} no encontrada'

    # Test con nombre de cuenta que debería coincidir
    account_0 = AccountBalance(
        account_name="Cuenta de productos Test 0",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_0 = SemanticContext.from_account(account_0)
    result_0 = rule.evaluate(ctx_0)
    assert result_0 is not None, f'Regla 'productos_agricolas' debería activarse para 'Cuenta de productos Test 0''
    assert result_0.semantic_type == "expense"
    assert result_0.confidence > 0.0

    account_1 = AccountBalance(
        account_name="Cuenta de agricolas Test 1",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx_1 = SemanticContext.from_account(account_1)
    result_1 = rule.evaluate(ctx_1)
    assert result_1 is not None, f'Regla 'productos_agricolas' debería activarse para 'Cuenta de agricolas Test 1''
    assert result_1.semantic_type == "expense"
    assert result_1.confidence > 0.0


def test_productos_agricolas_no_match():
    """No debería activarse para cuentas no relacionadas"""
    from semantic.semantic_rules import build_rules
    from models.account_balance import AccountBalance
    from semantic.semantic_context import SemanticContext
    from models.monetary_amounts import MonetaryAmounts

    rules = build_rules()
    rule = next((r for r in rules if r.name == 'productos_agricolas'), None)
    assert rule is not None

    account = AccountBalance(
        account_name="Cuenta No Relacionada",
        nature="loss",
        amounts=MonetaryAmounts(losses=1000.0),
    )
    ctx = SemanticContext.from_account(account)
    result = rule.evaluate(ctx)
    assert result is None

# Auto-generado por Knowledge Generator — NO incorporar directamente
# Tests para nuevas entradas de diccionario

from __future__ import annotations


# ---------------------------------------------------------------------------
# Tests para nuevas entradas de diccionario
# ---------------------------------------------------------------------------


def test_dictionary_entry_suelo():
    """Diccionario debería clasificar 'suelo' como ER.01"""
    from src.dictionary import DictionaryClassifier
    from models.account_balance import AccountBalance
    from models.monetary_amounts import MonetaryAmounts

    classifier = DictionaryClassifier()
    account = AccountBalance(
        account_name="suelo",
        nature="asset",
        amounts=MonetaryAmounts(assets=1000.0),
    )
    result = classifier.classify(account)
    assert result is not None
    assert result.standard_code == "ER.01"


def test_dictionary_entry_preparacion():
    """Diccionario debería clasificar 'preparacion' como ER.01"""
    from src.dictionary import DictionaryClassifier
    from models.account_balance import AccountBalance
    from models.monetary_amounts import MonetaryAmounts

    classifier = DictionaryClassifier()
    account = AccountBalance(
        account_name="preparacion",
        nature="asset",
        amounts=MonetaryAmounts(assets=1000.0),
    )
    result = classifier.classify(account)
    assert result is not None
    assert result.standard_code == "ER.01"


def test_dictionary_entry_camellones():
    """Diccionario debería clasificar 'camellones' como ER.01"""
    from src.dictionary import DictionaryClassifier
    from models.account_balance import AccountBalance
    from models.monetary_amounts import MonetaryAmounts

    classifier = DictionaryClassifier()
    account = AccountBalance(
        account_name="camellones",
        nature="asset",
        amounts=MonetaryAmounts(assets=1000.0),
    )
    result = classifier.classify(account)
    assert result is not None
    assert result.standard_code == "ER.01"


def test_dictionary_entry_administracion():
    """Diccionario debería clasificar 'administracion' como ER.01"""
    from src.dictionary import DictionaryClassifier
    from models.account_balance import AccountBalance
    from models.monetary_amounts import MonetaryAmounts

    classifier = DictionaryClassifier()
    account = AccountBalance(
        account_name="administracion",
        nature="asset",
        amounts=MonetaryAmounts(assets=1000.0),
    )
    result = classifier.classify(account)
    assert result is not None
    assert result.standard_code == "ER.01"


def test_dictionary_entry_gastos():
    """Diccionario debería clasificar 'gastos' como ER.01"""
    from src.dictionary import DictionaryClassifier
    from models.account_balance import AccountBalance
    from models.monetary_amounts import MonetaryAmounts

    classifier = DictionaryClassifier()
    account = AccountBalance(
        account_name="gastos",
        nature="asset",
        amounts=MonetaryAmounts(assets=1000.0),
    )
    result = classifier.classify(account)
    assert result is not None
    assert result.standard_code == "ER.01"
